# TSURU — Master Ecosystem Roadmap & Tracking

**Status:** LIVING DOCUMENT — this is the single tracking doc for the whole Tsuru ecosystem.
**Created:** 2026-06-11 (by the roadmap workflow, same day as the four approved workstreams).
**Owner:** tsuru.
**Upstream sources:** the full audit corpus in `docs/audit/tsuru/` (especially `tsuru_capability_inventory.md`, `tsuru_gap_analysis.md`, `tsuru_reconciliation_report.md`, `tsuru_product_strategy.md`, `tsuru_executive_vision.md`), the workstream specs in `docs/roadmap/` (`tsuru_rebrand_plan.md`, `tsuru_fairs_spec.md`, `tsuru_fairs_architecture.md`, `rbac_express_contract.md`), and `docs/RBAC_ORG_MODULES_PORT.md`.

---

## 1. How to read and update this document

1. **Every tracked item has a stable ID `TSR-NNN`.** IDs are never reused or renumbered, even when an item is closed or split. When splitting, keep the original ID on the parent and add new IDs for children (note the lineage in the row).
2. **Status values (exactly five):**
   - **Done** — shipped/verified (for code: implemented + type-checked/tested; deployment-and-rollout steps are tracked as their own items). For specs: the document exists and is approved.
   - **In progress** — actively being worked, or partially landed.
   - **Planned** — approved direction with a defined home (a workstream, phase, or spec section), not yet started.
   - **Not started** — known from the audit, no workstream covers it yet. These are the silent-drop risks — review §6 every update.
   - **Won't do** — explicit decision not to build (or to reword/retract a marketing claim instead). Always cite the decision source.
3. **To update:** change the Status + Evidence/next-step cells in §2; if a Not-started item gets a workstream, move its reference into §5 phases; if a promise disposition changes, update §3; append a dated line to the §8 changelog. Do not delete rows — strike through or mark Won't do.
4. **Source column codes:** `C#/S#/T#/O#` = gap-analysis IDs (`tsuru_gap_analysis.md`); `Top-10 #n` = its prioritized list; `P#n` = promise rows in `tsuru_reconciliation_report.md` §1; `Strat §x` = `tsuru_product_strategy.md`; `Vision` = `tsuru_executive_vision.md` §5; `Decision 2026-06-11` = the owner decisions that created the four workstreams (rebrand, fairs, RBAC-in-Express, trueque MVP).
5. **Authoritative decisions baked into this doc (2026-06-11):**
   - **Rebrand**: JMarkets → Tsuru on all user-facing surfaces; FREE e-invoicing becomes the headline pillar; NO domain/infra renames yet. Spec only so far (`tsuru_rebrand_plan.md`).
   - **Fairs**: real public fairs app (stands, joinable by orgs), own FastAPI backend (`fairs-be`) on the shared Postgres, stand editor as an independent POS section, managed from the Tsuru admin (landing admin) connected to the fairs BE — hybrid model, public landing stays 100% static (`tsuru_fairs_spec.md`, `tsuru_fairs_architecture.md`).
   - **RBAC**: implemented in the Express server (`E:/dev/BeautyMarket/server`) — overriding the older cross-app-be recommendation in `docs/RBAC_ORG_MODULES_PORT.md` §5. **Implemented as code in this session** (see §2.B), per `docs/roadmap/rbac_express_contract.md`.
   - **Trueque**: MVP integrated into the fairs spec (offers/proposals/WhatsApp handoff, no ledger/credits in v1; `settlementType` forward-compat column).

---

## 2. Ecosystem status board

Grouped by product pillar (pillar mapping follows `tsuru_product_strategy.md` §4, with rebrand/landing and platform-debt split out for tracking).

### 2.A — E-invoicing & POS core (Strategy pillars P1 + P2: the moat and the daily-use product)

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-001 | Hacienda v4.4 e-invoicing pipeline (clave/consecutive → XML → XAdES sign → ATV submit → validation poll → PDF → email/webhook) | Done | Inventory §3 (Live & mature) | `biller-apps/auth/app/sales-api/src/services/sales_pipeline.py`; SQS FIFO mesh `cloudformation/hacienda-messaging.yml`. Maintain; debt tracked in TSR-002/003/004/009. |
| TSR-002 | Fiscal credential hardening: encrypt P12/PIN/ATV password (KMS/Secrets Manager), stop returning them in GET | Not started | C4, O6, Top-10 #2 (Critical, legal) | Plaintext today in `auth/app/organization-configurations/src/models/organization_configuration.py`, returned per `auth/api-gateway/endpoints.json`. **No workstream owns this yet** — see §6. |
| TSR-003 | Env-drive the Hacienda OAuth realm (hardcoded staging `rut-stag` in shared code) | Not started | C4 | Production cutover risk for legal invoices. Pairs with TSR-002. |
| TSR-004 | Unify fiscal sources of truth: ONE consecutive allocator, explicit `sales`↔`billing_sales` relation, ONE canonical tax engine + cross-language contract tests pinning the POS TS mirror | Not started | C5, Top-10 #3, Strat §5.5 | Two allocators (`cross-app-be/app/models/consecutive.py` vs `jbiller_common/.../consecutive_repository.py`); tax math triplicated (`templates/pos-system/src/services/taxCalculationService.ts` + 2 Python engines). Drift = legally wrong invoices. |
| TSR-005 | Unblock the cashier loop: ~~register `/pos` route~~ (superseded — POS now lives inside the documents editor; the standalone `/dashboard/pos` route was deliberately RETIRED 2026-06-12 with a redirect to `/dashboard/documents`), mount `ClosingFlow.tsx`, server-side shift on session start | In progress (route part resolved differently) | C3, O2, Top-10 #4 | Remaining blockers: `components/pos/ClosingFlow.tsx` (450 lines of dead code with full backend support) + server-side shift. |
| TSR-006 | Wire POS checkout to the org's registered economic activities (replace hardcoded `activity_code='722000'`) | **Done** (2026-08-06) | C8, O3, Vision NOW | Checkout now reads the registered org's active Hacienda activities, auto-selects the first active code, renders an activity selector, and blocks submission when none is configured (`fe/pos-system/src/components/pos/checkout/{CheckoutDrawer.tsx,sections/DocumentSection.tsx}`). |
| TSR-007 | Stop trusting client-supplied `x-user-id` on sales-api (spoofable audit attribution on legal invoices; defaults `"anonymous"`) | Not started | C7, Top-10 #1 (part) | Replicate cross-app-be's gateway mapping `claims.sub → x-user-id` (`cross-app-be/api-gateway/template.yml:127-128`) to the jbiller gateway; also fix cross-app-be `UserIdMiddleware` guarding the wrong prefix. |
| TSR-008 | Remove plaintext password stash in POS sessionStorage (Register/Login post-OTP auto-login) | **Done** (2026-08-06) | C7 | Registration uses Amplify `autoSignIn`; verification stores only email/origin metadata and removes any legacy `verificationPassword` value. Login verification no longer persists credentials. |
| TSR-009 | Implement or remove the two jbiller stub endpoints: notification resend, XML regenerate | Not started | Inventory §3 (Scaffolded) | `auth/app/sales-api/.../sale_controller.py` acknowledged stubs. Low priority; track so it doesn't masquerade as Live. |
| TSR-010 | Enforce closing-approval authorization (`is_manager` currently defaults `True` "for backward compatibility") | Not started | Inventory §2 (Scaffolded) | `cross-app-be/app/services/closing_service.py`; natural follow-on once RBAC enforcement (TSR-027) reaches cross-app-be (TSR-031). |
| TSR-011 | Strip POS production console logging of full API response payloads | **Done** (2026-08-06) | T7, C6-adjacent | Removed request/response payload logging and component debug logs from `fe/pos-system`; API failures now use a typed `ApiError` without dumping sensitive response data. |
| TSR-012 | Login/Register redirect handling (`?redirect=`, `redirectAfterLogin`) + invitation Decline no-op | Not started | C7 | `templates/pos-system/src/Routes.tsx:72`, `Login.tsx:42-46` — invitation joins silently drop context. |
| TSR-013 | Fix `TODO(verify-endpoint)` POS confirmation routes + per-client delivery emails (hardcoded "Modas Laura" branding, static `EMAIL_RECIPIENT`) | Not started | Inventory §2 (Partial) | `cross-app-be/app/services/email_service.py`; substantially resolved by the cross-docking extraction (TSR-093). |
| TSR-014 | POS analytics endpoints on markets-api "still to verify" | Not started | Inventory §2, memory `project_pos_dashboard_migration.md` | POS `orgPath('/analytics')` consumers exist; backend unverified. |
| TSR-119 | POS i18n leaks: hardcoded Spanish in documents components (breaks EN mode) + dead `clients.orders.comingSoon*` keys | **Done** (2026-08-06) | QA analysis 2026-07-09 (`docs/qa/POS_FE_QA_ANALYSIS.md` §4) | Document actions, type filters, and sort labels now use ES/EN translation keys; dead `clients.orders.comingSoon*` keys were removed. |
| TSR-120 | POS orphaned/unrouted pages — product decision: migrate or delete | Not started | QA analysis 2026-07-09 | `fe/pos-system`: `src/pages/dashboard/AnalyticsPage.tsx` + `AssignmentsPage.tsx` (no route, no import) and the `src/pages/pos/*` cashier-device flow (documented in POS CLAUDE.md §5 but never wired into `Routes.tsx`). `ClosingFlow.tsx` stays tracked under TSR-005. |
| TSR-121 | POS repo hygiene: stale `.env.example` (pre-rebrand `*.jcampos.dev` API domains, "JMarkets POS" name) + boilerplate npm README in a pnpm repo | Not started | QA analysis 2026-07-09 | `fe/pos-system/.env.example`, `README.md`. Low risk but misleads new devs; code defaults were already fixed (`65f2ca9`). |
| TSR-122 | POS per-tab cart persistence — cart state is shared across document tabs (parallel editing mixes lines) | Not started | QA analysis 2026-07-09; POS `TASKS.md` T7.4-future | `fe/pos-system`: zustand cart store is global; TASKS.md calls for persist/restore per tab + `data.line_items` rehydration on tab activation. |
| TSR-123 | POS FE QA/product-owner analysis document (functional inventory, status, TSR incidences, bugs, QA plan) — md + xlsx | Done (2026-07-09) | Owner request 2026-07-09 | `docs/qa/POS_FE_QA_ANALYSIS.md` + `POS_FE_QA_ANALYSIS.xlsx` (one sheet per module; regenerate via `docs/qa/generate_pos_qa_xlsx.py` whenever the md changes — rule added to root `CLAUDE.md`). Deep-dive addendum same day: fiscal-engine + data-api catalog sheets (Motor-Fiscal, Catálogos-DataApi) → findings TSR-124/125/126. |
| TSR-124 | Hacienda v4.4 `Exoneracion` capture — no UI anywhere: missing from the line-detail form, and `Product` exemption fields (`exemption_authorization_code`/`exempted_rate`/`exemption_amount`/`iva_collected_factory`) round-trip in types with no editor | Not started | QA deep-dive 2026-07-09; POS `CALCULATION_AUDIT.md` §2 + gap #8 | `fe/pos-system`: hooks `useAllExemptions`/`useExemptionValidation`/`useAllExemptionIssuingInstitutions` (`useDataApi.ts`) exist and are consumed nowhere. Exonerated clients (state institutions, free-trade zone) cannot be invoiced correctly. |
| TSR-125 | Hacienda v4.4 `OtherCharges` document block (codes 01–10/99, ≤15/doc; code 04 requires third-party id) — absent in POS FE and sales BE; plus `Σ MedioPago == TotalComprobante` reconciliation not enforced in BE | Not started | QA deep-dive 2026-07-09; POS `CALCULATION_AUDIT.md` §1.5 + §4 | `fe/pos-system`: `useAllOtherCharges` unused; no checkout section. BE side needs model + DTO + service + routes (sales service). |
| TSR-126 | Audit POS `ReferencesSection` (NC/ND) against Nota 10/10.1 — full `ReferenceDocType`/`ReferenceCode` coverage + mandatory `Razon` | Not started | QA deep-dive 2026-07-09; POS `CALCULATION_AUDIT.md` §2 ("partial — needs spec audit") | `fe/pos-system/src/components/pos/checkout/sections/ReferencesSection.tsx`; wrong/missing reference data ⇒ credit/debit notes rejected by ATV. |
| TSR-127 | POS sale-receipt printing is unreachable — `PRINT_RECEIPT.md` documents a print button in `src/pages/pos/POSPage.tsx`, a file that no longer exists; the only `window.print` lives in `ReportePage`; checkout `Receipt.tsx` has no print action | **Done (code)** (2026-09-07) — **DB applied to dev** | QA shell sweep 2026-07-09 | Closed by building the ticket **server-side**, not in the browser. A `@media print` approach was written and then rejected: it would have meant two maquetadores for the same comprobante — the server's (which already renders the order PDF and the cross-docking report) and a new CSS one — so a reprint would stop matching the original, and the slip could never be emailed or reopened later. Now `app/templates/ticket.html` → wkhtmltopdf at 72mm (no page-height: the roll is continuous, and fixing one would pad short tickets with blank paper) → S3 → `attachments.ticket_url`, via `POST /orders/{doc}/ticket`. Deliberately **regenerates** each call so a ticket printed after invoicing picks up the consecutive, key and QR it did not have before; generated **on demand** because most orders are never printed. The heading names what the slip is — `PROFORMA` carries "No es un comprobante fiscal" under it, since a slip that looks like a receipt invites being treated as one. Migration `ac9d0e1f2a3b` applied to dev; 9 tests; `docs/PRINT_RECEIPT.md` rewritten against reality. Comandas per kitchen station remain (models exist). |
| TSR-128 | POS notifications have no backend feed or persistence — `NotificationsContext` is in-memory `useState` only (lost on refresh); the `source:"be"` type exists but nothing publishes it; this also blocks the catalog-invalidation feed (`useCatalogInvalidationFeed`) that rides the same pipeline | Not started | QA shell sweep 2026-07-09; POS `docs/CATALOG_INVALIDATION_TODO.md` | Future user-app notifications microservice (per the TODO doc) or an interim polling/SSE transport; until then catalog changes can lag up to 7 days (localStorage cache maxAge). |
| TSR-129 | Reconcile the two storefront-deploy pipelines the POS knows about: `useDeployments` surfaces the markets-api pre-deployments→publish→history pipeline (never exercised, `TODO(verify-endpoint)` on every route) while the REAL org-site deploy is the sales-be provisioner (TSR-118 W2, live) with **no POS UI** (no trigger, no status visibility) | Not started | QA sales/deploy sweep 2026-07-09 | Decide which pipeline the POS DeploymentsPage should surface; verify or retire the markets-api one; consider surfacing provisioner status (`organization_settings.infrastructure_status`) in the POS. |
| TSR-130 | **POS offline-sale replay is broken (confirmed bug):** `public/sw.js` `syncPendingSales()` sends `Authorization: Bearer ${sale.token}` but `useCartFlow.handleConfirmPayment` never persists a `token` field into `db.sales` (schema has it optional) → replay POSTs `Bearer undefined` and fails forever; sale stays `synced:false`, inventory already decremented. Even with the token stored, Cognito ID tokens expire (~1h) before a typical reconnection | **Done** (2026-08-06) | QA sales-subflow sweep 2026-07-09 | Replay moved out of the service worker into an authenticated foreground sync bridge that obtains a fresh Cognito token through `salesApi`. IndexedDB v2 tracks pending/syncing/synced/failed states, retry metadata, user ownership, payload and response. Immediate submit and replay share an `Idempotency-Key`; sales-be adds an org-scoped unique key plus an advisory transaction lock so retries return the original sale. Retriable failures show a queued-success receipt; permanent failures keep the cart and preserve local inventory. |
| TSR-131 | POS checkout receipt lifecycle — cart clearing zeroed the receipt summary and the document tab closed before the cashier could see the server result | **Done** (2026-08-06) | Drawer/offline audit 2026-08-06 | Checkout snapshots item count/total before submission, renders confirmed or queued result data, keeps the drawer/tab alive through the receipt, and removes the completed tab only when “Nueva venta” closes the receipt. |
| TSR-132 | POS drawers and full-screen dialogs can be clipped/hidden by scrolled or transformed page ancestors; nested overlays also race on body-scroll restoration | **Done** (2026-08-06) | Drawer audit 2026-08-06 | Shared `Drawer`, `Modal`, document-action modal, and mobile drawers portal to `document.body`. A shared overlay stack provides reference-counted body locking, topmost Escape/backdrop handling, focus trap/restoration, dialog semantics, `100dvh`, localized close labels, and a non-dismissible processing state. Covered by three drawer regression tests. |
| TSR-133 | Split the POS production bundle and avoid downloading unauthorized route code | **Done** (2026-08-06) | Build verification 2026-08-06 | Every public/dashboard page is a dynamic import; protected pages sit behind a fail-closed `PermissionBoundary` that resolves RBAC before rendering `Suspense`. Documents list/editor and the dashboard QR modal are split further. Main JS fell from ~1.49 MB / ~365 KB gzip to 475.21 KB / 129.08 KB gzip; no >500 KB warning. The generated manifest records pages as dynamic imports, while `index.html` preloads only shared vendor chunks. Three permission-loader tests prove denied/loading users do not invoke the protected import. |
| TSR-134 | Correct the cross-docking upload CDN from `tsuru-uploads.jcampos.dev` to `uploads.tsuru.jcampos.dev` and allow the origin across all backends | **Done** (2026-08-07) | Owner request 2026-08-07 | In PACIFIC-PROD (`947999370977`), `crossdocking-s3-stack` now retains and reuses bucket `tsuru-uploads-dev` plus distribution `E3SNH6UU8M0M3F`, with a new ACM certificate, Route53 alias, exact-domain Amazon CAA record, and corrected S3 CORS. cd-backend SSM now references the active distribution and corrected PDF domain. Explicit origin allowlisting shipped through data-be `main` (`1eca765`), management-be `main` (`989367f`), sales-be `develop` (`4895ae3`), and store-be `develop` (`63e2994`). Verified an existing PDF returns 200 through the new hostname with ACAO and data-api preflight returns 200. |
| TSR-135 | Replace the POS cross-docking PDF preview with a complete native order-detail view and correct the frontend/API field mapping | **Done** (2026-08-07) | Owner request 2026-08-07 | Removed the remote pdf.js iframe and rendered the orders-be report header plus every sale-point row directly from the order response. Corrected the POS DTO contract (`store_number`, ordered/sent/missing, `total_line_items`, box-summary fields), derives visible totals from displayed rows, omits the PDF's redundant fixed three-row label/report tail, and maps sale-point headers/footers to the order's stored report palette. Sale points are labeled **Cajas**, shipped unit-containers are **Bultos**, the article total counts unique product codes rather than repeated distribution rows, summary values are centered, and all five explicit downloads remain available. Report colors live in one theme module and the locale catalog is split/deduplicated into matching ES/EN domain JSON files with parity, common-copy, and source-key tests. |
| TSR-136 | Fully migrate POS static inline and runtime-injected legacy styles into the design system | **Done** (2026-08-07) | Owner request 2026-08-07 | Replaced static JSX layout styles across shared components and dashboard pages with centralized responsive grid/workspace/collapse/animation classes; removed component-rendered `<style>` blocks and DOM hover mutations; centralized storefront editor, avatar, document-type, and cross-docking palettes under `src/theme`/CSS variables. Truly dynamic data values (progress, runtime coordinates, user-selected branding/category colors, component sizing) remain explicit exceptions. Added an AST-based `pnpm check:styles` gate to reject new static inline declarations and scattered owned hex colors. |
| TSR-139 | **Branch-types catalog implemented in store-be** — the per-org catalog the POS has expected since TSR-111 never existed in any backend (API Gateway answered `403 Missing Authentication Token`, i.e. unknown route, while `/branches` correctly answered 401); the POS swallowed the failure into hardcoded `stand`/`restaurant` defaults | **Done (code)** (2026-08-21) — **not deployed** | Owner report 2026-08-20 ("branch-types orders endpoint is not responding") | New `branch_types` table + model/DTOs/repository/service/controller in `be/store-be` exposing the 4 routes the POS hook already calls (`GET`/`POST` collection, `PUT`/`DELETE` item). `code` is a slug, unique per org, and **immutable** (branches reference it by value); delete is refused with 409 while any branch still uses it. Migration `z6b7c8d9e0f1` also **drops the `check_branch_type` CHECK constraint** that still pinned `branches.type` to `('stand','restaurant')` — without that the catalog would have been decorative — and seeds each org from the codes its branches already use plus the two legacy defaults. 15 unit tests pass; API-Gateway template regenerated. Migration **applied and verified 2026-08-21** (6 rows seeded, constraint dropped, 0 orphaned branches). Next: deploy store-be `develop`. |
| TSR-140 | **`POST /branches/{code}/terminals` 500** — `create_terminal() missing 1 required positional argument: 'dto'`: the controller passed 3 args to a 4-arg service, so the request body landed in `branch_code` | **Done (code)** (2026-08-21) — **not deployed** | Owner report 2026-08-20 | `terminals_controller.py` now passes `branch_code`; `terminal_service.create_terminal` annotation corrected from the undefined `number` to `int` (only lazy annotations kept it from raising at import). The other five terminal endpoints already matched their service signatures. |
| TSR-141 | **Register-page step rail and brand-panel polish** — beside the brand-story panel the 3-step rail wrapped, leaving a connector segment dangling after "Verificar" and pushing the wrapped chip off-centre | **Done** (2026-08-21) | Owner report 2026-08-20 | New `stackedRail` prop on the shared `Stepper` keeps the two-row grid rail at every breakpoint (no single-row flex, no connectors); `Register.tsx` opts in. `BrandStoryPanel` narrative copy is justified with `hyphens-auto`. Also in the same pass: the gender "other → back to select" control now fires on `onMouseDown` with `preventDefault` (the focused input's blur used to re-render the button out from under the pointer, so the click needed several presses) and sits at `z-10`; `select.input`/`select.pp-input` gained explicit `color-scheme` + `option` colors so the OS-drawn option list stops falling back to the system palette (reported on macOS Edge — awaiting a screenshot to confirm the closed control renders correctly). |
| TSR-149 | **POS sent branch UUIDs where the branch APIs take the integer `code`** — `POST /branches/{branch_code}/terminals` from the POS session-setup screen (and the branch update/status mutations on the stations page) put `branch_id` in the path, so store-be answered 422 `int_parsing` on the UUID | **Done (code)** (2026-08-31) — **not deployed** | Owner report 2026-08-31 | `fe/pos-system` (`chepelcr/tsuru-pos-system`): `SessionSetupScreen.tsx` and `PuestosPage.tsx` now address branches by `branch.code`; the branch status toggle moved off the general PATCH (which has no `status` field) onto the dedicated `/branches/{code}/status` route. Root cause was a naming trap — `CreateTerminalRequest.branch_id` was a `number` holding a branch *code* while `Branch.branch_id` is a UUID string; the field was inert on the wire (the branch is in the path) and has been removed along with the now-unused `branchId` form props. `tsc --noEmit` clean. |
| TSR-150 | **Business type per organization** — every org saw the same POS regardless of what it sells; there was no way to offer a restaurant its mesas or a minisuper its price checker without showing them to everyone | **Done (code)** (2026-09-07) — **DB applied to dev** | Owner request 2026-09-07 | `organizations.business_type` (9 values) + two *independent* flags `is_retail_supplier` and `is_pyme` — deliberately NOT type values, because a minisuper can be a registered PYME *and* supply a chain, and a single-value column cannot express that. `syncBusinessTypeModules` in `management-be` resolves the **union** of type + flags and writes `organization_modules`, enabling granted modules and *disabling* (never deleting) revoked ones. `rbac-seed.ts` gained 9 vertical modules + 24 submodules; `quotes` joined `DEFAULT_ORG_MODULE_NAMES`. POS: `useBusinessType` reads the module list directly and **fails closed** — `usePermissions().hasModule()` was unsuitable because it fails *open* while loading and auto-grants to owners (right for a permission, wrong for a vertical: a minisuper owner must not see Mesas). Shared `BusinessIdentityFields` in org settings **and** wizard step 1, which now reveals in beats. Migration `0019` + `db:reseed-rbac` applied to `tsuru/dev` 2026-09-07: 18 modules, `quotes` reached all 8 orgs, **0 orgs gained a vertical module** (nothing switches on until a type is chosen). Next: deploy `management-be`. |
| TSR-151 | **Pedido manual capture was thin** — free-text delivery point, an unused campaign field, a Payment section that does not belong on an unpaid order, and no way to write the order number or attach a B2B department | **Done (code)** (2026-09-07) | Owner request 2026-09-07 | A `PM` checkout is now **two cards**: Pedido and Receptor. The Documento card is not collapsed for a `PM`, it is **absent** — sale condition, activity code and currency+rate were extracted to `checkout/fields/` and moved onto the Pedido card, and its *Notas* was deleted rather than moved because `useCartFlow` already merged it into *Comentario*. Payment is gone entirely. The card leads with a **proforma toggle**, then a writable order number. Delivery point became a three-mode switch — registered point / receiver's address / structured CR cascade via the shared `LocationSelect` — with **no free-text-only path left**; validation requires a `store_id` or a real address. "Evento o campaña" removed. Currency and rate stayed **one** component: the auto-rate lock, pinned CRC/USD/EUR order and rate-unavailable guard are one coupled behaviour. |
| TSR-152 | **The manual-orders endpoint did not exist** — every pedido manual failed or sat in the offline outbox forever (`docs/MANUAL_ORDERS.md` §10); `OrderDetailTotals` could not even express a discount or tax total | **Done (code)** (2026-09-07) — **DB applied to dev** | `MANUAL_ORDERS.md` §10 | `POST /orders` discriminates on `source`, **by hand rather than a Pydantic Union**, so the storefront body — which predates the field and never sends it — keeps validating untouched (verified). A new path was rejected: the offline outbox persists an absolute `syncUrl`, so changing it would orphan queued pedidos. Totals recomputed server-side from the lines (a client sending `line_total: 999999` gets the real figure); user-writable `document_number` with 409 on collision; `Idempotency-Key` dedup via a partial unique index. **Missing structures added**: `OrderDetailTotals` now exposes `discounts`/`taxes` (the model always had them — the DTO simply never surfaced them, so a caller could not reconcile a total), and lines gained `product_id`, `cabys`, `net_price` and JSON tax/discount breakdowns. Also built `POST /orders/{doc}/invoice`, without which a delivered pedido could be billed twice. Migration `aa7b8c9d0e1f` applied to dev. |
| TSR-153 | **Two POS UI defects** — the payment "Más" menu revealed nothing, and every select showed the bare OS chevron flush against its border | **Done (code)** (2026-09-07) | Owner report 2026-09-07 | The "Más" bug was a **CSS-layer bug, not overflow logic**: `.dropdown-menu` was declared *outside* `@layer`, so at equal specificity it beat every utility on the element — the portaled menu got `position:absolute`, `z-index:30` and `display:flex` instead of `fixed z-modal grid`, painting it behind the drawer (`z-drawer` 200). Moved into `@layer components` (verified in the built CSS: `.dropdown-menu` at byte 7764, utilities after it at 14089) and re-pointed at `z-drawer-modal`; the offline fallback now lists all 8 Hacienda payment codes instead of leaving one item behind "Más". Selects: a native `<option>` honours almost nothing (the popup is OS-drawn), so built `SelectField` — a portaled, keyboard-navigable, ARIA combobox listbox — and re-pointed the shared `Select` at it with an `<option>`-children compat layer, converting **32 files without rewriting a single handler**. Chevron moved to 12px/40px for anything still native. Added the `prefers-reduced-motion` guard the codebase lacked. |
| TSR-154 | Restaurant vertical — mesas, combos, modificadores, cuenta dividida, servicio 10%, comandas | **Done (code)** (2026-09-07) — admin CRUD screens pending | Owner request 2026-09-07 | Mesas full-stack (model/repo/service/controller/DTOs + `useTables` + `TablesPanel`), addressed by **integer `branch_code` per TSR-149** because the POS session only holds the code. `tables.is_dynamic` doubles as the bar's open tab — a tab *is* a table, so the bar vertical needed no second mechanism. **Combos explode at add-to-cart**: a combo mixing a 13% item with an exempt one cannot be one flat line on a fiscal document, so the discount distributes proportionally (the only split preserving each component's taxable ratio) and is clamped at zero so an over-priced combo cannot raise a component's base — 11 tests. Servicio 10% is an **itemized line, never a markup**, and excludes itself from its own base (it would otherwise compound on every recompute). Split-bill shares must reconcile to the original bill; unassigned lines go to share 0 rather than being dropped. |
| TSR-155 | Product code lookup + minisuper vertical | **Done (code)** (2026-09-07) | Owner request 2026-09-07 | **Code lookup ships ungated to every organization** — scanning is how a POS is used, so it rides `commercial/read/products` rather than a vertical module; `find_by_company_and_any_code` matches the number across every code type via JSONB containment (verified against live dev data: `ART001` resolved correctly). Offline mirror gained the same rule so a till behaves identically without signal. Scale barcodes (weight/price embedded in EAN-13) parse through a parameterised pure helper — the layout is per-shop, not standardised — with 8 tests. |
| TSR-156 | Proformas / cotizaciones for every organization | **Done (code)** (2026-09-07) | Owner request 2026-09-07 | **Not a new document type**: a proforma is an order in `quote` status, so approving one is a status change and "Facturar pedido" needs no special case. Ungated — a `quotes` default module, not a vertical or a PYME privilege. Transition guard on both sides blocks **`quote → delivered`**: the one that matters, since it would let an unapproved cotización be invoiced as though the customer had agreed. Entry point is a toggle at the top of the Pedido card, not a second footer button. |
| TSR-157 | **Cross-docking sale points could only be created by uploading a spreadsheet** — a supplier with the distribution figures but no Excel file could not record them | **Done (code)** (2026-09-07) | Owner request 2026-09-07 | CRUD beside the parser (`/orders/{doc}/crossdocking/sale-points`), writing the same rows, so a partial upload can be finished by hand and an Excel-imported point can be renamed to fix a typo. **Totals are derived** from the items and the product's `units_per_box`, never sent, so a hand-captured order reconciles against an imported one and the existing report/PDF work unchanged. Over-allocating a product beyond what the order line ordered returns 422 **naming the line** — the check the spreadsheet path gets free from its template. |
| TSR-158 | Bar vertical — cuentas abiertas, precios por horario | **Done (code)** (2026-09-07) — schedule CRUD UI pending | Owner request 2026-09-07 | Tabs reuse `tables.is_dynamic`. Happy-hour resolution handles **windows that wrap midnight** (22:00–02:00 is the normal shape; a naive `start <= now <= end` never matches one) and **never raises a price above catalog** — a misconfigured "discount" is a misconfiguration, not a surcharge. The server re-resolves on submit: price is authority-critical and a stale or tampered client clock must not set it on a fiscal document. 10 tests. |
| TSR-159 | Farmacia vertical — lote/vencimiento, controlados, recetas | **Done (code)** (2026-09-07) — controlled-register UI pending | Owner request 2026-09-07 | FEFO (**First Expiry, First Out**, not FIFO — for anything with a shelf life the date that matters is when it goes bad, not when it arrived). Undated stock sorts **last** so it cannot jump ahead of something about to expire; `0` days means "expires today" — a warning, not its absence. `controlled_sales` is append-only: a register that can be edited is not a register. |
| TSR-160 | Servicios vertical — servicios por hora, facturación recurrente | **Done (code)** (2026-09-07) — EventBridge scheduler + UI pending | Owner request 2026-09-07 | Monthly cadence **clamps instead of overflowing**: the 31st + 1 month is the 28th, so a subscription cannot skip February. Overdue schedules are included, not skipped — if the scheduler missed a day the customer still expects the invoice. A run creates a **draft**; emitting a fiscal document is a decision a person makes, not a cron job. **This is the only piece needing new deployment infrastructure** (store-be is Lambda with no scheduler today). |
| TSR-161 | Ferretería vertical — unidades con conversión, pedidos especiales | **Done (code)** (2026-09-07) — unit CRUD UI pending | Owner request 2026-09-07 | Stock in rollos, sell by the metro. Reaches the tax engine only through `quantity` and `unit_price`, so the Hacienda cascade is untouched — which is why the vertical is cheap. A zero/negative conversion factor is rejected rather than silently zeroing inventory movement; an explicit `price_override` wins, because deriving a cut metre's price from the roll loses money on every cut. |
| TSR-162 | Agenda vertical — salón **and** taller | **Done (code)** (2026-09-07) — calendar UI pending | Owner request 2026-09-07 | One module with two consumers, not two calendars: a salón books a person, a taller books a person **and** a vehicle (nullable `asset_id`). Commission precedence resolved in code rather than a SQL scoring expression, so the rule people argue about when a payout looks wrong is readable in one place. |
| TSR-163 | Taller vertical — órdenes de trabajo as an order type | **Done (code)** (2026-09-07) — OT screen pending | Owner request 2026-09-07 | **A work order is not a new entity** — it is a `PM` order with `order_type='work_order'`. Parts and labour are both just lines; the status chain already matches a taller with only render-time labels differing (Recibido / En reparación / Listo / Entregado); the cotización step *is* TSR-156's `quote`; and "Facturar pedido" closes it unchanged. The one genuinely new thing is `client_assets`, scoped to the client like departments and stores — a table rather than a JSON blob because per-asset history ("what did we do to this car last time") only exists if the asset outlives the visit. |
| TSR-164 | **Feria del agricultor is a multi-org grouping feature, not a business type** | Not started | Design decision 2026-09-07 | A feria is a *collective of independent vendors*, so the real feature is grouping and settlement across many organizations (shared event, per-vendor sessions, consolidated reporting). It does not fit `business_type` at all, and modelling it as one would paint us into the wrong shape. Deliberately **absent from the picker** rather than present-and-inert. |

### 2.B — Security, RBAC & multi-tenancy (Strategy pillar P3: precondition for everything multi-tenant)

**This is the pillar the 2026-06-11 sessions changed.** The RBAC-in-Express contract (`docs/roadmap/rbac_express_contract.md`) was fully implemented and verified across three parallel workstreams (server, POS UI, landing admin); all type checks/builds pass; 51/51 RBAC unit tests green. The deployment session then **applied the migration + seeds to the live DB, committed/pushed everything (monorepo + standalone repos), and realigned the catalog 1:1 with the POS sidebar** (TSR-037). The catalog reseed was executed and verified on 2026-06-11 — **the live DB now serves the new sidebar-mirroring catalog**. Rollout step 1 is complete; next gates are TSR-028/029 and the enforce flip (TSR-027).

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-020 | RBAC schema port: `submodule_actions`, `organization_modules`, `organization_submodules` entities + `roles.isActive`; migration generated | **Done** (this session; migration apply pending → TSR-026) | Decision 2026-06-11; contract §1; `RBAC_ORG_MODULES_PORT.md` §3-4 | `server/src/entities/{SubmoduleAction,OrganizationModule,OrganizationSubmodule}.ts`, `Role.ts`, `entities/index.ts`; `migrations/0001_new_katie_power.sql` (reviewed; `verification_token` drift made idempotent). |
| TSR-021 | Org-scoped RBAC endpoints O1–O15 (my-permissions, available-matrix, role CRUD hardened, bulk permission set with V2 subset rule, member-role assignment with V3, check-permission/user-role fixed) + V1/V4/V6 service rules + 60s cache | **Done** (this session) | Contract §3.1, §4 | `server/src/services/RBACService.ts`, `controllers/RBACController.ts`, `repositories/{RBACRepository,OrganizationModuleRepository}.ts`; guards verified mounted per-route; org-spoof and probe holes closed; V3 also enforced in `MembershipService` + `InvitationService`. |
| TSR-022 | Platform-admin surface P1–P18 (`/api/admin`: org search, module assignment/overrides/apply-defaults, module/submodule/action catalog CRUD, submodule-actions matrix) + real `requirePlatformAdmin` (users.role check) + `attachUserId` | **Done** (this session) | Contract §3.2, §3.0 | `server/src/services/PlatformRBACService.ts`, `controllers/PlatformAdminController.ts`, `middleware/permissions.ts` (orgId param fix at all 3 readers, `RBAC_ENFORCEMENT` flag), `routes.ts` `/api/admin` mount, `dependency_injection.ts` wiring. DI smoke test passed. |
| TSR-023 | RBAC seeds: `upload` action, `submodule_actions` matrix, `DEFAULT_ORG_MODULE_NAMES` + default assignment on org create, org-modules backfill script | **Done** (this session; seed runs pending → TSR-026) | Contract §5 | `server/src/seeds/rbac-seed.ts`, `seeds/org-modules-backfill.ts`, `scripts/run-org-modules-backfill.ts`; `package.json` `db:generate`/`db:seed:org-modules`. |
| TSR-024 | POS org-scoped roles management UI: RolesPage + RoleDrawerForm + org-filtered PermissionMatrix (O2-driven), member role assignment (O11), `usePermissions()` gating, sidebar item, full ES/EN i18n (~55 keys/lang) | **Done** (this session; E2E pends backend deploy) | Decision 2026-06-11; contract §6 (POS) | `templates/pos-system/src/{hooks/useRbac.ts,types/rbac.ts,pages/dashboard/RolesPage.tsx,components/roles/*}` + `orgRbacPath` in `lib/api.ts`; tsc + vite build pass; endpoints cross-checked one-for-one against RBACController. FE is fail-open while `RBAC_ENFORCEMENT=log`. |
| TSR-025 | Landing admin (Tsuru admin) platform RBAC section — the FIRST BE-connected admin section, proving the hybrid model: org list/detail, catalog, actions, matrix pages behind `PlatformAdminGate` (Cognito + `users.role==='platform_admin'`) | **Done** (this session) | Decision 2026-06-11 (hybrid model); contract §6 (landing) | `landing-client/src/{lib/admin-api.ts,repositories/platform-rbac.repository.ts,services/platform-rbac.service.ts,admin/rbac/*,admin/pages/Rbac*.tsx}` + manifest/sidebar/i18n (90 keys); prod-bundle leak check clean (no `api/admin`/`__local` strings); public site untouched. **This unlocks the fairs admin pattern (TSR-063).** |
| TSR-026 | RBAC rollout step 1 — apply migration `0001_new_katie_power.sql`, run `db:seed` + `db:seed:org-modules`, bootstrap admin access | **Done** (2026-06-11 deploy session) | Contract §1/§5 | Migration applied via `db:run-migration` (journal anomaly workaround, TSR-102); seeds + org-modules backfill ran (84 submodule_actions, 35 org_modules across 5 orgs — old catalog; superseded by TSR-037 reseed). `platform_admin` user bootstrap replaced by `ADMIN_AUTH_MODE=open` for now (TSR-039). |
| TSR-037 | **RBAC catalog realignment** — seeded catalog now mirrors the POS sidebar 1:1 (modules `panel/documents/commercial/admin/storefront/reports`; submodules = sidebar items; **pos folded into `documents/emitted`**, no separate pos module); `submoduleActionMatrix` + `rolePermissionMatrix` remapped; RBACController guards `team`→`admin`; destructive reseed script `db:reseed-rbac` (preserves roles/memberships, aborts on custom org roles unless `--force`) | **Done** (live reseed executed 2026-06-11) | User feedback 2026-06-11 (drawer showed old modules) | `server/src/seeds/rbac-seed.ts` (rewritten), `src/scripts/run-rbac-reseed.ts`; rule documented in `server/CLAUDE.md` + POS `CLAUDE.md` §5.1. **Live DB verified post-reseed**: 6 modules / 18 submodules / 68 submodule_actions / 30 org_modules (6 defaults × 5 orgs) / 100 role_permissions (module-wide null-submodule grants — resolved as module-wide by `RBACRepository`); system `admin` role has 26 grants incl. `admin` module (roles UI reachable); chepelcr@outlook.com membership intact as `admin`. |
| TSR-038 | **POS nav gated by user-org role** (legacy `validar_permiso` parity): `NAV_PERMISSION` map ([module, submodule] per NavId), `itemVisible()` hides items/sections without read, Documentos + "+" create button gated; fail-open until my-permissions resolves (log-mode rollout); `chepelcr@outlook.com` mapped to system `admin` role in modas-laura for end-to-end testing | **Done** (committed, pushed to `chepelcr/tsuru-pos-system`) | User request 2026-06-11 | `templates/pos-system/src/components/layout/DashboardSidebar.tsx` (mobile drawer reuses it); membership `eefeb66f-…` role updated in live DB (org owner_id unchanged). Full gating fidelity arrives with the TSR-037 reseed + TSR-027 enforce flip. |
| TSR-107 | **Fine-grained `organization` module** — twin of the `admin/organization` sidebar item: 9 submodules mirroring the POS org-settings cards 1:1 (fiscal-info, hacienda, notifications, theme, general, branding, contact, payment, shipping; read/update only); granted to platform_admin/owner/admin templates; POS cards filtered by `can('organization','read',cardId)`; BE settings routes gated per-section (GET→read, writes→update; settings/theme→`branding`) | **Done** (live reseed executed 2026-06-12) | Owner request 2026-06-11 ("org page configs as sub-submodules") | `server/src/seeds/rbac-seed.ts`, `src/routes.ts` (orgSettingsGuard); POS `OrgSettingsPage.tsx`; rule documented in both CLAUDE.md files ("fine-grained twin exception"). |
| TSR-108 | **Org-settings persistence fixes** — `organizations` gains `description/email/phone/address` (General section) + scalar `theme` (POS shell theme id; root cause of "theme not persistent": the column and the PATCH route didn't exist, so saves 404'd); `PATCH /organizations/:id` added + update field whitelist (closes ownerId/plan mass-assignment); new `PATCH /settings/general`; payment/shipping/contact/branding endpoints verified end-to-end (full upsert CRUD existed); POS TODO(verify-endpoint) comments resolved | **Done** (migration 0002 applied 2026-06-11) | Owner report 2026-06-11 (theme not persistent across sessions) | `server/src/entities/Organization.ts`, `migrations/0002_thin_nomad.sql`, `OrganizationController.ts`; POS `useOrganization.ts`/`useOrgSettings.ts`. Hacienda/notifications/fiscal-info persist via sales-api (already working). |
| TSR-109 | **Per-doc-type create permissions** — `documents` gains doc-type submodules (`fe/te/nc/nd/fc/fexp`) as pure create-gates; `rolePermissionMatrix` supports `'module/submodule'` keys; staff (cashier) template restricted to `documents/emitted` + `fe` + `te` (no credit/debit notes, purchase, export); POS create menus (sidebar "+", documents toolbar, dashboard quick actions) filter by `useCreatableDocTypes()` | **Done** (live reseed executed 2026-06-12) | Owner request 2026-06-11 (cashiers must not create NC/ND) | `server/src/seeds/rbac-seed.ts` (submodule-specific grants), POS `types/invoice.ts` (`permSub`), `hooks/useRbac.ts`, 3 create surfaces. |
| TSR-110 | **Full-POS RBAC action-gating sweep** — 6-area workflow audit (12 agents) gated 133 actionable elements app-wide (create/edit/delete/export/upload/invite/publish buttons, bulk bars, kebab menus, drawer submits, empty-state CTAs) with hide-not-disable + fail-open convention; catalog gap fixed (`commercial/confirmations` gains `create`, live via idempotent seed). Roles drawer: doc types render as a nested picker under Emitidos→Crear (not submodule rows); full catalog + system-role labels localized ES/EN via `rbac.*` keys + `rbacI18n.ts` (displayName fallback keeps custom roles user-named) | **Done** (deployed 2026-06-12) | Owner request 2026-06-12 ("complete audit… hide actions the user can't make") | POS `6141c61` (58 files), server `5457b58`; workflow run `wf_264d21f9-c97`. Remaining known gap: IssuedReceivedToggle list filter intentionally left ungated (filter, not action). |
| TSR-111 | **POS shell RBAC/UI polish** — sidebar footer now shows the **org-scoped role** from `my-permissions` (localized via `roleLabel`) instead of the platform-level `user.role` (which is `customer` on first Cognito sync — admins saw "customer"); notifications panel centered as a fixed overlay on mobile (was `absolute right-0`, clipped off the left viewport edge); read-only system-role drawer now shows the role description (localized `roleDescription` + `roles.form.noDescription` fallback) | **Done** (this session) | Owner report 2026-06-12 (admin shown as "customer"; notif panel cut on mobile; no description in default-role view) | POS `src/components/layout/{DashboardSidebar,NotificationsBell}.tsx`, `src/components/roles/RoleDrawerForm.tsx`, `LanguageContext.tsx` (+1 key ES/EN); tsc clean. |
| TSR-039 | **`ADMIN_AUTH_MODE=open`** — anonymous `/api/admin` access (server `requirePlatformAdmin` bypass) + landing `VITE_ADMIN_AUTH_MODE=open` gate bypass with amber banner; TEMPORARY until a dedicated **admin Cognito pool** ships (planned: new Cognito template, then flip both envs back) | **Done** (temporary measure) / pool **Planned** | Owner decision 2026-06-11 ("admin cognito will be a new cognito template, for now anonymous") | `server/src/middleware/permissions.ts`, `server/.env(.example)`; `landing-client/src/{lib/admin-api.ts,admin/rbac/PlatformAdminGate.tsx}`. **Never set `open` on a publicly hosted admin build** (see R8). Note: the `tsuru-cognito` template (TSR-114) + the `cognito-custom-emails` pattern (TSR-115) are the substrate for the eventual admin pool. |
| TSR-115 | **Custom Cognito emails** — branded, ES/EN HTML for sign-up/forgot-password/attribute-verify/admin-create-user via a CustomMessage trigger Lambda backed by SES templates, in a NEW standalone repo `chepelcr/tsuru-cognito-templates` (Python/container, own GH Actions OIDC deploy). | **Done** (2026-06-20; deployed, pool wired) | Owner request 2026-06-20 (custom Cognito mail templates) | New repo `be/cognito-templates/` (working copy): Lambda `tsuru-dev-cognito-templates` (Active) maps trigger+`locale`→SES template `tsuru-dev-cognito-{logical}-{lang}`, rewrites `{{code}}`/`{{username}}`→`{####}`/`{username}`, graceful fallback; 8 SES templates uploaded; `tsuru-cognito` `LambdaConfig.CustomMessage` wired. Built from the new reusable **`cognito-custom-emails`** skill (`~/.claude/skills/`), itself derived from the fire-code pattern. Deploy-role gotchas fixed: `cloudformation:ListExports`, `iam:GetRolePolicy`, `ecr:SetRepositoryPolicy` + the ECR-repo Lambda-pull policy. |
| TSR-116 | **Cognito email via SES `tsuru@jcampos.dev`** — pool switched from `COGNITO_DEFAULT` to `DEVELOPER` sending with the verified `jcampos.dev` SES identity. | **Done** (2026-06-20) | Owner note 2026-06-20 (tsuru.dev already a verified SES identity) | `Infrastructure/cognito/tsuru-cognito.yml` `EmailConfiguration` (From `tsuru@jcampos.dev`, SourceArn `…identity/jcampos.dev`); redeployed + verified on pool `us-east-1_0cTNJ31eo`. **SES still in sandbox** → verification mails only reach verified recipients until production access is requested (new §7 step). |
| TSR-027 | RBAC rollout step 2 — flip `RBAC_ENFORCEMENT` from `log` (default) to `enforce` after the POS roles UI is in use; consider switching the POS `usePermissions` to fail-closed at the same time | Planned | Contract §3.0.4; implementer follow-ups | `requireMembership`/`requirePlatformAdmin` are hard gates regardless of the flag. Pre-req: TSR-026 + TSR-035. |
| TSR-028 | Verify `cloudformation/api-gateway.yml` proxies `/api/admin/*` (and the new `/rbac/*` sub-routes) WITH the Cognito authorizer attached, before any hosted landing-admin build | Planned | Contract §6; verification report | `/api/admin` relies on gateway JWT signature validation + server-side `users.role` check. |
| TSR-029 | userId↔JWT-`sub` path matching at the markets-api gateway (the documented-but-absent claim-to-path check) | Not started | C1, Top-10 #1; Inventory §4 (Missing-but-promised) | `BeautyMarket/api-gateway/template.yml` has only a plain authorizer. `attachUserId` (TSR-022) *consumes* the path userId trusting this gateway check — closing it is load-bearing for the whole O1–O15 surface in production. **No workstream owns it** — see §6. |
| TSR-030 | Finish + deploy the jbiller lambda-authorizer membership check on all 4 gateways (the convergence point for ecosystem-wide authorization) | Not started | C1, O1, Top-10 #1; explicitly out of scope in contract §6 | TODO stub at `auth/app/lambda-authorizer/src/validators/organization_auth_checker.py:73-85` (role hardcoded `"member"`). Express now has its own membership checks (TSR-021); sales-api/orders-api/data-api still have none. |
| TSR-031 | Route-level enforcement in cross-app-be and jbiller against the new RBAC model (per-org modules → POS feature gating beyond markets-api) | Not started | `RBAC_ORG_MODULES_PORT.md` §9; contract "out of scope" | Follows TSR-026/027/030. The `walmart`-style "special module" use case (Modas Laura) is the canonical driver. |
| TSR-032 | Shared-DB contract hardening: per-service DB roles or real RLS (today permissive `using true`), schema-contract tests across the 4 writer repos, single master for `organizations` | Not started | T3, C5-org, Top-10 #9, Strat §5.9 | The fairs-be `DB_CONTRACT.md` pattern (`tsuru_fairs_architecture.md` §3.1) is the template to retrofit onto existing services. |
| TSR-033 | Org-membership enforcement across the rest of the Express org-scoped routes (beyond `/rbac/*`) | In progress | C1; this session | `attachUserId` is now mounted on the whole org-scoped router and `requirePermission` honors `RBAC_ENFORCEMENT=log`; hard membership gates currently exist on the RBAC routes only. Completing = mounting guards per-route across products/CMS/settings controllers, then TSR-027. |
| TSR-034 | Secrets-in-repo hygiene: Cognito pool ID + HostedZoneId committed plaintext in two gateway templates (violates repo's own rule) | Not started | T7 | `auth/api-gateway/template.yml`, `data-services/api-gateway/template.yml`. |
| TSR-035 | Grant the system `admin` role template the roles permissions so non-owner admins can reach the roles UI under enforce mode | **Done — superseded by TSR-037** | Verification report (design note) | The realigned catalog moved roles under the `admin` module and the system `admin` template carries `admin:['create','read','update','invite']`. Takes effect with the TSR-037 reseed. |
| TSR-036 | `GET /api/admin/organizations/:orgId` (P1bis) so landing-admin deep links show the org name without list-cache handoff | Planned | Verification report (remaining issue) | Cosmetic; landing falls back to showing the raw orgId today. |

### 2.C — Rebrand & landing (JMarkets → Tsuru; messaging repositioned on the free billing engine)

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-040 | Rebrand plan (identity, messaging architecture, full change inventory, phased checklist, RIBCA gating) | **Done** (document; spec-only per decision) | Decision 2026-06-11; S6, O9, Top-10 #10 | `docs/roadmap/tsuru_rebrand_plan.md`. No content changes applied yet — execution is TSR-041..043. |
| TSR-041 | Phase R1 — landing content execution: brand swaps (branding/navbar/footer/seo), hero/features/about rework, NEW billing pillar section (free e-invoicing as message #1), honesty pass (fairs→roadmap framing, testimonials/traction/phone/newsletter/24h removed), legal de-claim pass, optional palette swap | **Done** (2026-06-12; workflow-applied in `fe/landing`) | Rebrand plan §3, §5 R1; fixes P#10/16/17/18/19/20 + partial 21–23 | All content JSONs swapped; hero leads with "Vende legal/fácil/comunidad"; NEW `billing.json` pillar wired (4 admin pieces + `/facturacion` prerender route); fairs→"en construcción", testimonials/"miles"/phone/newsletter/24h removed; GDPR/encryption de-claimed; emails → `*@tsuru-markets.org`; Tier-1 natural-pigment palette in `themes.json`; 3 hardcoded literals fixed (`navbar.tsx`/`footer.tsx`/`About.tsx`). Verified: `pnpm check`+`build`+prerender green, admin tree-shaken (no `__local`), 0 user-visible JMarkets. Logo/wordmark slots still empty (no asset yet). |
| TSR-042 | Phase R2 — POS strings: `index.html` title, `usePageTitle` BRAND, `Logo.tsx` fallback, `auth.login.title` (ES+EN), theme display names ("Tsuru"/"Tsuru Demo"; **IDs unchanged**) | **Done** (2026-06-12; applied in `fe/pos-system`) | Rebrand plan §4.3, §5 R2 | Title/BRAND/Logo fallback → "Tsuru POS"; `auth.login.title` ES+EN → "Tsuru"; theme display names → "Tsuru"/"Tsuru Demo"; default theme recolored to the Tsuru palette (theme IDs `jmarkets`/`jmarkets-demo` + `DEFAULT_THEME_ID` kept — DB-persisted). Verified: `tsc --noEmit` + `vite build` green. Residual: one JSDoc comment in `appCode.ts` (non-user-facing, optional). |
| TSR-043 | Phase R3 — docs sweep: "Tsuru (formerly JMarkets)" in living docs; audit corpus untouched (historical record); add a "Brand: Tsuru" note to root CLAUDE.md | **Done** (2026-06-12) | Rebrand plan §5 R3 | Root `CLAUDE.md` brand note added + `fe/dashboard` paths; living docs swept (`docs/README.md`, `docs/LANDING_CLIENT_BRIEF.md`, `docs/app/MULTI_TEMPLATE_ARCHITECTURE.md`) with brand-vs-identifier rule (domains/buckets/template IDs left intact); audit corpus untouched. Template/deploy archive docs left as-is (mostly infra identifiers). |
| TSR-044 | Domain/infra renames (j-markets.tsuru.dev → Tsuru domain, buckets, `seo.json siteUrl`, admin CTA URLs, theme/template ID data migrations) | **Done** (2026-06-20; forced by + subsumed into the account migration TSR-114) | Decision 2026-06-11 (deferred) → executed 2026-06-20 | Domains cut over: `tsuru.jcampos.dev` (landing), `app.tsuru.jcampos.dev` (POS), `api`/`data-api`/`sales-api`/`orders-api`.`tsuru.jcampos.dev`; `seo.json siteUrl` + landing POS CTAs → `app.tsuru.jcampos.dev`; SAM buckets/ECR/SNS/SSM/stack names → `tsuru-*`. Theme/template **IDs left intact** (DB-persisted; brand-vs-identifier rule). See TSR-114. |
| TSR-045 | Tier 2 Bribri cultural identity (lexicon IA, Ú-sure/cacao logo mark, etnogeometric patterns) | **Mostly gated** (narrow slice shipped — see TSR-113) | Rebrand plan §1.3 | **Still blocked until real RIBCA/Bribri consultation/partnership exists** for lexicon-as-IA, the Ú-sure/cacao logo mark, and etnogeometric patterns. Tier 0/1 (name + story + palette) shipped with R1. **Exception (owner decision 2026-06-13):** a narrow, attributed Sibö/cacao cosmology narrative now appears on POS registration (TSR-113) — factual, reverent, credited to Bribri culture, no sacred claims made on Tsuru's behalf. |
| TSR-113 | Sibö/cacao origin-story panel on POS registration (side narrative beside the sign-up form; attributed Bribri cosmology) | **Done** (2026-06-13) | Owner request 2026-06-13; advances a slice of TSR-045 | `fe/pos-system`: `AuthLayout.tsx` gains an optional `aside` split; new `components/common/BrandStoryPanel.tsx` (token-only styling); `Register.tsx` renders it; `LanguageContext.tsx` `auth.story.*` keys (ES+EN). tsc + build green. Copy is factual + attributed ("inspired, with respect, in the Bribri culture of Talamanca") — deeper Tier-2 (lexicon IA, logo mark, patterns) stays gated (TSR-045). |
| TSR-046 | Re-aim landing Login/Register CTAs from the legacy dashboard (`admin.j-markets.tsuru.dev`) to the POS app (`pos.j-markets.tsuru.dev`) — landing pages + branding.json + server invitation emails | **Done** (2026-06-11; committed, pushed to `chepelcr/tsuru-landing` + `chepelcr/tsuru-platform-api`) | C6, Top-10 #5; reconciliation §3.5 | Landing: navbar, About/Comunidad/Examples/Ferias/Funcionalidades/Landing, `branding.json` adminLoginUrl/adminRegisterUrl. Server: `InvitationService.ts` fallback URL → pos.j-markets (`/join/:token`). Scope per owner: "landing and emails" only — the `admin.` DNS redirect itself stays with TSR-091. |
| TSR-047 | Real contact-form delivery (SES path exists in 3 backends, or a jbiller API-key-scoped endpoint) | Planned | C6, O4; P#18 | Rebrand R1 removes the false 24h promise NOW; this item restores a working form later. `landing-client/src/pages/Contact.tsx:24-38` fakes submit today. |
| TSR-048 | Full legal-pages rewrite with counsel (privacy/cookies/terms describing actual practice; cookie banner only if analytics ever ship) | Planned | P#21/22/23; rebrand plan §2.4 | R1 does the minimal de-claim pass; this is the follow-through. |
| TSR-049 | Remove landing dead auth code (~290-line `useAuth.ts`, `auth-navbar.tsx`, multi-tenant `apiUtils.ts` dragging aws-amplify into the static bundle) | Not started | T8 | Note: Amplify is now legitimately used by the admin RBAC gate (TSR-025) — scope this to the *public* bundle only. |
| TSR-050 | Auto-translate setting (scaffolded, no implementation) — implement or remove the setting | Not started | Inventory §6 (Scaffolded) | `settings.json autoTranslate {enabled:false}`. |
| TSR-051 | Brand asset generation guide (logo system, loading patterns, color/typography specs, prompt-ready briefs for generating Tsuru logos and page assets) | **Done** (document) | Owner request 2026-06-11 ("guidance document so I can generate my logos") | `docs/roadmap/tsuru_brand_asset_guide.md`. Asset production itself is owner-driven; generated assets land via rebrand R1/R2 (TSR-041/042). Tier 2 Bribri marks remain gated (TSR-045). |

### 2.D — Fairs & community / trueque (Strategy pillar P5: the staged greenfield that justifies the brand)

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-060 | Fairs product specification (concept, lifecycle, participation, 4 stand types, POS Ferias section, admin management, trueque MVP, journeys, MVP cut, risks) | **Done** (document) | Decision 2026-06-11; S1, P#12/13 | `docs/roadmap/tsuru_fairs_spec.md`. Converts the landing's worst honesty row (Fairs 1/10) into a real build. |
| TSR-061 | Fairs technical architecture (new `fairs-be` FastAPI service, `fairs_*` tables + shared-DB ownership contract, 3-tier API, theme inheritance, preview modal, S3 assets, build order M1–M6, security checklist) | **Done** (document) | Decision 2026-06-11 | `docs/roadmap/tsuru_fairs_architecture.md`. Own backend per decision; shared Postgres; cross-app-be house pattern via `be-builder` + companion skills. |
| TSR-062 | Fairs M1 — `fairs-be` skeleton + schema (`fairs_*` tables, read-only Organization/Product mappings, `alembic_version_fairs`, `DB_CONTRACT.md`, local run + lambda-local tests) | Not started | Fairs arch §11 M1 | First implementation milestone. Repo: `chepelcr/tsuru-fairs-be` (or incubate in monorepo per `monorepo-folder-split`). |
| TSR-063 | Fairs M2 — admin vertical: admin endpoints + Tsuru admin "Ferias (online)" pages (fairs CRUD, stand-type catalog, dev-mode Cognito login), seed 4 stand types + 1 permanent fair | Not started | Fairs arch §11 M2; spec §8 | **Dependency: the BE-connected admin pattern + PlatformAdminGate shipped in TSR-025 is the template** (manifest `online:true` rows, admin-api layering, Cognito gate). Server-side `platform_admin` enforcement in fairs-be per arch §4.3. |
| TSR-064 | Fairs M3 — merchant vertical: org-scoped endpoints + POS Ferias section (join flow, master StandProfile + per-fair stand editor with sections/checklist, presigned uploads, publish gate, preview modal with two tabs + viewport toggle, theme inheritance) | Not started | Fairs arch §11 M3; spec §6-7 | POS conventions: `fairsApi` client + `fairsOrgPath`, `useFairs.ts`, NAV item, `fairs.*` i18n namespace (arch §8). Stand defaults to the org's store template theme (decision 2). |
| TSR-065 | Fairs M4 — public vertical: fairs SPA (`tsuru-fairs-client`: directory/floor/stand, scoped theme tokens, WhatsApp CTA, metrics beacons) + S3/CloudFront deploy + api-gateway stack | Not started | Fairs arch §11 M4, §6 | Hostname `ferias.j-markets.tsuru.dev` for now (rename rides TSR-044). Mobile-first 2D card grid; no auth for visitors. |
| TSR-066 | Fairs M5 — Trueque MVP: barter offers/proposals, two-sided accept, transactional WhatsApp handoff, trueque filter/badge, public reports + admin moderation queue, auto-expiry | Not started | Decision 2026-06-11 (trueque); spec §9; S2, P#11 | No ledger/credits in v1; `settlement_type` column reserved for the v2 mutual-credit path (spec §9.6). Report+moderate ships WITH the MVP, not after. |
| TSR-067 | Fairs M6 — hardening + launch: metrics rollup, fair analytics, rate limiting on public POSTs, landing `/ferias` copy flip from "en construcción" to live links | Not started | Fairs arch §11 M6 | **Dependency: rebrand R1 (TSR-041) must land first** so the fairs launch messaging flips from honest-roadmap copy, never from the current false present tense. |
| TSR-068 | Buyer/community-member identity (accounts for non-sellers) | Planned (Later) | S3, P#14 partial; Vision LATER | Deliberately NOT in fairs v1 (visitors stay anonymous; WhatsApp is the only conversion). Revisit after fairs ship. |
| TSR-069 | Cross-org marketplace discovery directory | Planned (folded into fairs) | S4, O8, Top-10 #10 | The fairs directory + permanent fairs (spec §2: `fair.type='permanent'`) **is** the first credible discovery surface; a standalone "discover stores" page remains an option later. |
| TSR-070 | Mutual support networks (mentors, collaborators) — promise P#14 | **Won't do (v1)** | Rebrand plan §2.4 ("Kill — no workstream exists") | Copy removed in R1; may return with the community layer post-fairs. |
| TSR-071 | Mutual-credit / ledger trueque (platform currency) | **Won't do (v1)** — documented future path | Spec §9.6 | Argentine Red Global de Trueque lessons recorded; v1 schema (`settlement_type`) keeps the door open with zero migration cost. |
| TSR-072 | Fair-trade "Garantizamos…" claim (P#10) | **Won't do (as a guarantee)** → Reword | Rebrand plan §2.4 | R1 rewords to "Trabajamos por condiciones justas…" until RBAC + marketplace rules can enforce anything. |
| TSR-073 | Cultural preservation / fair-story archive surface (beyond the static blog) | Planned (Later) | S8; Vision LATER (provenance/story CMS components, `womenLed`/`cooperative` org badges) | After fairs v1; CMS component catalog is the natural substrate. |
| TSR-074 | Canastas comunitarias (cross-org bundles), Jala-de-Piedra crowdfunding, cooperative org type + governance | Planned (Later) | S7; Vision LATER | Gated on enforced RBAC (TSR-027+) and fairs v1 traction. |

### 2.E — Storefront / commerce (Strategy pillar P4: repair-then-maintain)

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-080 | Public read-only product/category endpoints for live orgs (unbreaks storefront browse; converts promises #1/#3/#5 toward Delivered) | Not started | C2, O5, Top-10 #7 | Storefronts call `/api/public/organizations/{id}/products|categories` which don't exist (`server/src/controllers/PublicOrgController.ts` vs `templates/jmarkets-demo/src/lib/api.ts:62-81`); products live in cross-app-be. Decide which backend serves the public read. |
| TSR-081 | Minimal consumer-order record (grounds promise #6 "order tracking") OR formally relabel storefronts as catalog+WhatsApp brochures | Not started (decision pending) | C2, P#6, Top-10 #7 | Rebrand R1 already replaces the order-tracking feature card with a true one; the build-vs-relabel decision remains open — see §6. |
| TSR-082 | Real CMS publish pipeline (today `DeploymentService.ts:58-88` uploads `config.json` and instantly records success — simulated) | Not started | C2; Inventory §6 (Partial/simulated) | Contradicts the Transparencia value; pairs with TSR-092 (one provisioner). |
| TSR-083 | Collapse the 8 demo storefront apps to 1–2 maintained codebases with theme presets | Planned | T4, Strat §5.7; Vision NEXT | Cuts the build/deploy matrix ~8×; removes duplicated client-side CR geo data. Fairs theme work (TSR-064/065) reuses the same 28-token registry — coordinate. |
| TSR-084 | Publish the solidarity pricing model BEFORE any Stripe activation (permanent free micro-seller tier; transparent paid tiers for custom domains/multi-branch) | **Model published (landing surface Done)** — Stripe still not activated | S5, Strat §6; P#8/#9 guardrail | 2026-08-23: four-tier solidarity model live on the landing — `fe/landing/src/content/plans.json` + `/planes` (`/pricing` alias) + home teaser + `/admin/plans` editor (four pieces wired). Tiers **Semilla** (₡0 forever) / **Cosecha** / **Cooperativa** / **Feria** (custom). The "gratis" promise is protected by an explicit promise block: e-invoicing free on every tier, no sales commission, free data export, no expiry. **Amounts are preliminary** — `config.draftPricing: true` renders an "under review" notice; flip it off in the admin once final. **CTAs are inert** (`config.ctaComingSoon: true` → "Próximamente") until TSR-145 ships. **Two compliance corrections (2026-08-24):** all receipt types (FE/TE/NC/ND/FC/FEX) **and** ATV contingency mode moved into the free Semilla tier — both are legally required capability, not packaging, so they can never be gated (see the non-negotiables table in `tsuru_plans_implementation.md` §1). Stripe remains schema-only (`organizations.stripeCustomerId`, `plan`, no SDK). |
| TSR-142 | Relocate `templates/pos-landing` → `fe/pos-landing` (it is a product marketing site, not a storefront template) | **Done** | This session (2026-08-23) | `git mv` into the `fe/` grouping alongside `landing`/`pos-system`/`dashboard`. Stays **tracked** in the monorepo (no standalone repo exists — GitHub has no `pos-landing` under `chepelcr` or the orgs). Refs repointed: root `package.json` (`dev:pos-landing`/`build:pos-landing`, dropped from `build:templates`), `vite.config.ts` outDir → `dist/pos-landing`, both buildspecs, `scripts/pipeline-build.sh`, `scripts/pipeline-deploy.sh`, `scripts/pipeline-frontend-pos-landing.sh`. Build verified green from the new path. Its `public/config.json` pricing (Free/Pro, ₡20k mes · ₡200k año, addons, billing toggle) is the seed for TSR-084's tiers. |
| TSR-143 | Reserve the incoming Tsuru logo slot on the landing (wordmark still in production) | **Done** | This session (2026-08-23); brand asset guide §2 (L1/L2) | `fe/landing/src/components/layout/brand-logo.tsx` — renders `branding.logoUrl` / `logoUrlDark` (light/dark swap) when set, otherwise the botanical placeholder at the identical footprint, so dropping the asset in causes **no layout shift**. Wired into navbar (32px) + footer (28px). Going live needs no code change: upload via admin Media, set the URLs on Site identity. |
| TSR-144 | Business-card QR for `tsuru.jcampos.dev` | **Done** | This session (2026-08-23) | Generated with the POS's own `qrcode@1.5.4` (the engine behind `fe/pos-system/.../QrShareModal.tsx`) so the card QR and the merchant-facing QR come from one tool. Assets + specs + regenerator in `docs/brand/qr/` (brand / brand-on-white / mono, SVG + 2048px + 512px). Version 4, 33×33 modules, EC **H**; **min 21 mm printed, 25 mm recommended**. All raster variants decode-verified with jsQR. |
| TSR-145 | Implement the plans model in the BE + org creation + POS (entitlements, limits, upgrade UX) | **Spec written, not built** | This session (2026-08-24) | Full implementation spec: `docs/roadmap/tsuru_plans_implementation.md`. Key decision: **entitlements are a second axis from RBAC** (RBAC = which *user*; entitlements = what the *org* bought) — deny as **402 + requiredPlan**, never 403, so "hit your limit" never reads as "permission denied". New tables `plans` / `plan_entitlements` / `organization_subscriptions` (`limitValue NULL` = unlimited; per-org `overrides` implement Feria + grandfathering); plan activation *writes* the existing `organization_modules` rather than duplicating it. Rollout mirrors RBAC's `PLAN_ENFORCEMENT=log→enforce`. Org creation gets **no plan step** — every org starts Semilla. Steps 1–7 ship a working tier system with **zero Stripe**. |
| TSR-147 | CR pricing market research — validate the draft tiers against what merchants can actually buy | **Done** | This session (2026-08-24) | `docs/roadmap/tsuru_pricing_market_research.md`. Tsuru's real category is **POS + e-invoicing (US$20–80/mo)**, not pure invoicing ($5–20). At ₡452/USD: Cosecha $44.25/mo ($36.87 annual) sits mid-band and is **defensible** (Alegra POS Pro $50, POSMOVI Básico $45, Wolk $35) — the bundled storefront is what justifies it. **Cooperativa was above the entire market ceiling** (₡36.000/mes — Alegra POS Plus and POSMOVI Premium both land there). **Decided + applied 2026-08-24:** Cooperativa → **₡35.000/mes · ₡315.000/año**, and the annual discount widened 2 → **3 free months (25%, matching Alegra)** → Cosecha **₡180.000/año**. Doc rewritten **colón-first** (competitors converted *into* colones; no USD on any public surface). **IVA decided 2026-08-24: prices are FINAL, IVA included** — stated beside the amounts and in the FAQ (a 13% appearing later would be exactly the hidden charge "sin cargos ocultos" denies). This makes the comparison *better* than recorded: GTI quotes "+ IVA", so its real cost is 13% over list. **No-USD decided 2026-08-24:** customer-facing surfaces quote **colones only**, both languages. Rationale is the customer, not simplicity — Tsuru is only usable by a Hacienda filer, so there is no foreign-currency segment; and card networks convert at charge time with the issuer's own rate + FX fee, so any USD figure we printed would be systematically low against the real statement. The live exchange-rate service stays in the **POS**, where Hacienda requires the BCCR rate on USD invoices. `fe/pos-landing`'s USD toggle is **parked** (site not deployed) — delete the display if revived, don't wire a live rate into it. **All research items now closed.** |
| TSR-148 | Soft doc cap + 80% warning + document packs | **Spec'd (in TSR-145 §5.6); packs ride TSR-146** | This session (2026-08-24) | Owner decision 2026-08-24: `docs.perMonth` is a **soft cap** — warn at 80%, never 402 on emission. Market research independently supports it: Hacienda's **TicoFactura is free and uncapped**, so a hard cap sends merchants to a government product rather than to a paid tier. Overflow sells as non-expiring **document packs** (₡2.500/25, ₡8.000/100 — priced as a bridge so upgrading wins on the merchant's own arithmetic past ~250 docs). New `document_packs` table, FIFO consumption after the monthly allowance, no `expiresAt` (an expiring pack is a hidden charge). |
| TSR-146 | Stripe activation (payment collection only) | Not started — gated on TSR-145 | This session (2026-08-24) | Deliberately separated: plan assignment + enforcement work without any payment integration, and Feria never touches Stripe. Stripe becomes the source of truth for *payment state only*; entitlement resolution never calls it on the request path. `past_due` must go to a grace period, not an instant downgrade. TSR-084's "publish before activating" guardrail is now satisfied. |
| TSR-085 | Storefront gateway checkout / payments intermediation | **Won't do** | Strat §3 ("explicitly do NOT build gateway checkout until demanded"); P#25 | The ToS facilitator stance is the site's most honest claim and a real scope shield. WhatsApp handoff stays the model (storefronts AND fairs). |
| TSR-086 | QR catalog as a public feature (today QR exists only inside the POS admin) | Not started | P#5 (Partial) | Becomes meaningful once TSR-080 makes the linked catalog real. |
| TSR-087 | Pan-LatAm / multi-country fiscal expansion | **Won't do (deferred)** | S5, Strat §6 audience #4 | Only CR=188 seeded; placeholder personas removed in R1. Second country only after CR is consolidated. |

### 2.F — Platform & infra debt (consolidation plan + ops posture)

| ID | Item | Status | Source | Evidence / next step |
|---|---|---|---|---|
| TSR-090 | Finish the repo split: migrate CI off monorepo paths, untrack `templates/pos-system` + `landing-client` + `server`, kill the duplicate landing deploy path | **Done** (2026-06-20) | T1, O10, Top-10 #8, Strat §5.1 | All THREE folders untracked (2026-06-12); each repo's GH Actions owns its deploy. **2026-06-20 (TSR-114):** the obsolete monorepo CodePipeline IaC was deleted — `cloudformation/codepipeline.yml`, `Infrastructure/policies/pipeline-roles.yml`, `deploys/deploy-pipeline.sh`+`deploy-codepipeline.sh`, all 8 root `buildspec-*.yml` (`git rm`, commit `001d965`). All 6 repos now deploy solely via their own GitHub Actions. §7 step 1 cleared. |
| TSR-091 | One admin surface: deprecate `dashboard/`, delete `client/`, redirect `admin.j-markets` to the POS app | In progress | T2, Strat §5.2 | POS already absorbed the dashboard domain (memory `project_pos_dashboard_migration.md`); `client/` formally deprecated; the deployed `dashboard/` retirement + redirect remain. Includes TSR-046. Note: the "Tsuru admin" future is the landing admin promoted (per `RBAC_ORG_MODULES_PORT.md` §goal-4) — TSR-025 started that promotion. |
| TSR-092 | One infra provisioner + one templates store (winner: jbiller `infrastructure-service-provider`; retire `deploys/setup-template-bucket.js` provisioning + markets-api `templates` duplication) | Not started | T4, O7, Strat §5.3 | The strangler step is half-done (SNS-driven path live with self-healing re-emit). |
| TSR-093 | Extract the Modas Laura cross-docking business out of cross-app-be (own service/repo; stop `_sync_organization` fabricating orgs with `owner_id='system'`) | Not started | Strat §5.4; T4; C5-org | Purifies the POS backend AND removes the dual-mastering of `organizations`. Candidate first consumer of the per-org "special module" assignment (TSR-031). |
| TSR-094 | Decide markets-api's end-state + delete dead scaffolding (`HomePageContent`, `lambda.cts` SQS stubs, Stripe/PayPal keys without SDKs, stale CLAUDE.md claims of Product/Order controllers + `requireAuth`) | In progress (decision partially made) | T5, Strat §5.6 | **The RBAC decision commits markets-api as the P3 control plane short-term** (identity/org/RBAC now live there with real wiring). Dead-scaffolding cleanup + doc-truth pass still pending. |
| TSR-095 | Tame data-services sprawl (34 Lambdas → few/one catalog service; delete all-TODO cache layer + localhost-only write surface or expose it properly) | Not started | T6, Strat §5.8 | |
| TSR-096 | Merge the duplicated shared Python libs (`jbiller_common` vs `jmarkets_common`, identical `HaciendaApiClient` copies) | Not started | T4, Strat §5.5 | |
| TSR-097 | Frontend test coverage | **In progress** (POS started 2026-08-06) | T7 | POS now has Vitest/jsdom infrastructure plus 5 passing regression tests covering portaled drawers (backdrop, Escape, nested body lock) and queued-sale replay (authenticated idempotent success + retry retention). Landing/dashboard/storefront coverage remains outstanding. |
| TSR-098 | Repair pre-existing broken server tests: `organizationContext.test.ts` imports a deleted middleware (suite fails to load); 5 Organization create tests assert pre-onboarding behavior; OrganizationService test mock needs `findModuleByName` + `orgModuleRepo` stubs | Not started | Verification report (pre-existing, proven at HEAD) | Fail identically without the RBAC diff. |
| TSR-099 | Resolve the 139 pre-existing dashboard/ TypeScript errors (snake_case Product/Category typing drift from uncommitted user work) | Not started | Verification report (pre-existing) | server/, POS, landing all at 0 errors. Likely moot if TSR-091 retires `dashboard/`. |
| TSR-100 | Mirror monorepo POS/landing/server changes to the standalone repos (`chepelcr/tsuru-pos-system`, `chepelcr/tsuru-landing`, `chepelcr/tsuru-platform-api`) | In progress (continuous during split limbo) | Root CLAUDE.md split rules | 2026-06-11: all RBAC/rebrand-session changes committed + pushed to all three standalone repos (the standalone repos own the LIVE deploys now — GH Actions/Pages). Monorepo copies are transitional; mirroring stays a per-change duty until TSR-090 untracks the folders. |
| TSR-101 | Observability beyond the jbiller authorizer (structured logging, usage metrics for the new /api/admin + rbac surfaces) | Not started | T7; Inventory §9 (Partial) | RBAC denials are logged in `log` mode — a natural first metrics feed. |
| TSR-102 | Migration journal hygiene: journal entry `0000_swift_prodigy` has no .sql on disk (older migrations were hand-applied via `scripts/run-migration.ts`) — reconcile the drizzle journal so `db:migrate` works cleanly | Not started | Verification report (pre-existing condition) | Workaround documented in §7 step 1. |
| TSR-103 | Mobile wrapper (Capacitor/Ionic CORS origins allowed, no app exists) and real-time push | **Won't do (now)** — backlog | Inventory §9 (Scaffolded / Missing-not-promised) | Revisit only with a concrete product driver (e.g. fairs notifications). |
| TSR-104 | **Server split → `chepelcr/tsuru-platform-api` (PRIVATE) + GitHub Actions OIDC deploy** — `server/` extracted per `monorepo-folder-split` (self-contained: own package.json/tsconfig/drizzle.config/migrations/scripts; `vite.ts` degrades to API-only); keyless deploy via shared OIDC provider + per-repo role `tsuru-platform-api-dev-github-deploy`; 3-phase workflow (package lambda zip → `lambda update-function-code` on `jmarkets-dev-api-handler` → swagger → SAM deploy `jmarkets-dev-api-gateway`); swagger generator fixed (relative globs — absolute Windows globs silently yielded 0 paths; 88 paths now) | **Done** (green end-to-end run 2026-06-11) | Owner decision 2026-06-11; T1, Strat §5.1 | `server/cloudformation/deploy-role.yml`, `server/.github/workflows/deploy.yml`, `scripts/generate-swagger-spec.cjs`; pattern added to the `gh-actions-aws-deploy` skill (Variant B). **Follow-up: disable the now-redundant monorepo CodePipeline backend stages (§7).** |
| TSR-105 | tsuru-platform-api pnpm migration (npm lockfile EBADPLATFORM in CI: nested `@esbuild/aix-ppc64` optional-flag loss) — `preinstall only-allow pnpm`, `onlyBuiltDependencies:[esbuild]`, `--frozen-lockfile` in CI, vite ^7 devDep for vitest 4 | **Done** (2026-06-11; CI green) | CI failure → owner request | `server/package.json`, `pnpm-lock.yaml`, workflow uses `pnpm/action-setup@v4`. All server commands are `pnpm run …` now (`server/CLAUDE.md`). |
| TSR-106 | CORS allow-list = the Tsuru frontends on the platform API | **Done** (updated 2026-08-07) | Owner request 2026-06-11 → 2026-08-07 | `be/management-be/src/config/app.ts`. 2026-06-20 (TSR-114): replaced legacy frontend origins with `https://tsuru.jcampos.dev`, `https://app.tsuru.jcampos.dev`, and `https://admin.tsuru.jcampos.dev`. 2026-08-07 (TSR-134): explicitly added `https://uploads.tsuru.jcampos.dev` to management, data, sales (including API-key management), and orders/store backend CORS sources; all four Lambda fleets redeployed and the data-api preflight succeeds live. First-party wildcard regexes remain as defense in depth. |
| TSR-112 | Frontend `fe/` grouping (mirror of `be/`): relocate `landing-client`→`fe/landing`, `templates/pos-system`→`fe/pos-system` (gitignored standalone repos), and `dashboard`→`fe/dashboard` (tracked, `git mv`) | **Done** (2026-06-12) | Owner request 2026-06-12 | Folder moves + `.gitignore` markers updated; monorepo path refs repointed (`package.json` dev/build scripts, `tsconfig.json` include + `@/*`, root `vite.config.ts` root/alias, `buildspec-frontend-dashboard.yml`, `deploys/deploy-params.sh`); dashboard build verified → `dist/dashboard` (outDir is `__dirname`-relative, unchanged). Names strip the `tsuru-` prefix like `be/`. Obsolete landing/pos monorepo buildspecs left for TSR-090 removal. |
| TSR-114 | **Whole-ecosystem migration to the PACIFIC-PROD account + Tsuru-domain cutover** — the dead `J-CAMPOS` account (creds invalid) forced a fresh stand-up of everything in account `947999370977` with `tsuru-*` naming. Subsumes TSR-044 (domains) and finishes TSR-090 (CodePipeline retired). | **Done** (2026-06-20; APIs + landing + POS live & verified) | Owner request 2026-06-19/20 (platform down, only landing up) | **Foundation:** `tsuru-cognito` (fresh pool `us-east-1_0cTNJ31eo`, users re-onboard), `tsuru-dev-iam-policies/-roles` (reused the account's existing GitHub OIDC provider), SAM+uploads buckets, Secrets Manager `tsuru/dev/database` (Supabase), SNS `tsuru-dev-organization-events`, SSM `/tsuru/dev/{platform,commondata,hacienda}/*`. **Backends** (each repo repointed: stacks/Lambda/ECR/SSM/Cognito→`tsuru-*`, deploy-role + `AWS_DEPLOY_ROLE_ARN` secret, pushed): `api`/`data-api`/`sales-api`/`orders-api`.`tsuru.jcampos.dev` all live (custom domains reachable; management full-chain 200). **Frontends:** landing `tsuru.jcampos.dev`, POS migrated S3→GitHub Pages `app.tsuru.jcampos.dev` (own Pages workflow + CNAME + 404 fallback). **DNS** in zone `Z08953202H0791MTWJHTP`; pruned recycled IaC (backup `E:/dev/tsuru-infra-unused-backup-2026-06-19/`). Gotchas: ACM CAA blocked by the GitHub-Pages CNAME parent → per-subdomain CAA records permitting amazon (no wildcard possible); IAM SSM scope `parameter/tsuru/*`→`/tsuru/*`; deploy-roles needed `route53:ListHostedZonesByName`; image-Lambda needs an ECR repo policy for `lambda.amazonaws.com`. Dashboard intentionally skipped (TSR-091). **Carryover unchanged:** R1–R4/TSR-002/003/004/029/030/034 security debt was preserved, not fixed, by the move. |
| TSR-118 | **Storefront-deploy MVP** — per-org site auto-deployed at `{slug}.stores.tsuru.jcampos.dev`, consuming ONE shared public API (a generated API-Gateway routing to the existing lambdas, IaC in `tsuru-infrastructure`, anonymous-Cognito protected), with per-template repos + GH-Pages examples + structured-CR-address carts; plus a landing rebalance (POS/store/community harmony). Full map: `docs/roadmap/tsuru_storefront_mvp_milestone.md`. | **In progress** (W3 done 2026-06-20) | Owner request 2026-06-20 (revised); resolves TSR-080; relates TSR-083/092 | **Decisions:** public-api = **generator + combined gateway template** (NOT a new service) pointing to existing lambdas, in `chepelcr/tsuru-infrastructure`, manual SAM deploy; protected by an **anonymous Cognito identity pool** (guest); org domain `*.stores.tsuru.jcampos.dev` + 1 wildcard cert (**W3 ✅**, dodges the Pages-CNAME CAA trap); templates = per-template repos + GH-Pages examples + user-personalized; **org id injected at deploy**; org stays a path param (header deferred); **no new public controllers** (middleware audit: anonymous already works). Workstreams W1 public-api generator · W2 provisioner re-enable+repoint+inject · W3 wildcard cert ✅ · W4 template contracts · W5 per-template repos · W6 landing harmony · W7 anon Cognito · W8 GH-Pages examples · W9 cart CR-location · W10 product types/offer flag + filters (store-be `type` enum + `is_offer`) · W11 finish-pedido → tracked order (new public POST, grounds TSR-081) · W12 `programs` template-gated dashboard submodule · W13 landing blog posts open. |
| TSR-117 | **Returning-user org re-link** — DB user PK is the Cognito `sub`; the fresh pool issues a new `sub`, so re-registering orphaned the old org memberships. On sync (profile auto-sync + `completeEmailVerification`), a same-email match under a different id now transactionally re-points all user FKs (members.user_id/invited_by, invitations.invited_by, modules.assigned_by) to the new sub and drops the stale row — orgs reappear on first login. No schema migration. | **Done** (2026-06-20; CI green, lambda live) | Forced by TSR-114's fresh Cognito pool | `be/management-be/src/repositories/UserRepository.ts` (`relinkCognitoId`), `src/services/UserService.ts` (`syncUserFromCognito`), `src/types/repositories.ts` (commit `c9a964c`). Match key = **email** (register with the same email). |
| TSR-137 | **Registration broken by a dead Cognito trigger** — the `CustomMessage` trigger Lambda `tsuru-dev-cognito-templates` failed `Runtime.InvalidEntrypoint` on every invoke, so Cognito could not build the verification message and sign-up errored out | **Done** (2026-08-21; live) | Owner report 2026-08-20 (500 on register) | Root cause: the function is configured `x86_64` but was pinned to image digest `251939ec…`, which is **arm64** (built on Apple Silicon without `--platform linux/amd64`). A correct amd64 image had been pushed to `tsuru-cognito-templates-ecr:latest` on 2026-06-22 but the function was never repointed (last modified 2026-06-20). Fixed with `lambda update-function-code --image-uri …:latest` → resolves to `507dc3f7…` (amd64); smoke-invoked with a synthetic `CustomMessage_SignUp` event and it now returns the rendered HTML mail. **Follow-up:** the image build/push step must pin `--platform linux/amd64` (or the function switch to arm64) so this cannot recur. |
| TSR-138 | **Cognito→DB sync 500 on username collision** (extends TSR-117) — `syncUserFromCognito` re-linked an existing row by **email only**, so a Cognito user whose `preferred_username` was already taken by a different row hit `users_username_unique` and the profile endpoint returned an unhandled 500 | **Done (code)** (2026-08-21) — **not deployed** | CloudWatch `tsuru-dev-api-handler` 2026-08-20 22:50 UTC (`chepelcr70@gmail.com` / `JCampos`) | `UserService.syncUserFromCognito` now resolves the first free username variant (`name`, `name2`, …) before inserting; it deliberately does **not** re-link on a username match, since unlike email a username is not proof the records are the same person. `UserController.getProfile` maps Postgres `23505` to 409 instead of 500 as a backstop. 4 new `UserService` tests pass (the 7 remaining suite failures are the pre-existing Organization ones — TSR-098). Next: deploy management-be `main`. |

---

## 3. Broken-promise tracker (the 14 "Not built" verdicts)

Per `tsuru_reconciliation_report.md` §1 (tally: 8 Delivered · 4 Partial · 1 Built differently · **14 Not built**). Rule from the rebrand plan §2.4: **kill, fix, or move to an honest public roadmap — nothing stays in false present tense.** Every row resolves to a tracked item.

| Promise | Reconciliation verdict | Disposition | Tracked by |
|---|---|---|---|
| #6 Order tracking "at every step" | Not built | Replace feature card with a true one (R1); build a minimal order record OR relabel — open decision | TSR-041, TSR-081 |
| #10 Fair-trade "Garantizamos…" | Not built | **Reword** (aspirational verb, no guarantee) until enforcement exists | TSR-072 → TSR-041 |
| #11 Barter/trueque (present tense) | Not built | **Build (MVP)** in the fairs workstream; copy moves to "muy pronto" meanwhile | TSR-066; copy TSR-041 |
| #12 Fairs (3 types) | Not built | **Build** — the fairs application | TSR-060..067; copy TSR-041 |
| #13 Fair registration flow | Not built | **Build** — participation apply/approve in fairs spec §5 | TSR-062/063/064 |
| #14 Mutual support networks | Not built | **Kill** (copy removed; may return with the community layer) | TSR-070 → TSR-041 |
| #16 "miles de emprendedores" traction | Unsubstantiated | **Kill**; replace with the verifiable UCR origin credential | TSR-041 |
| #17 Testimonials (placeholder personas) | Unsubstantiated | **Kill**; only real, consented CR merchant stories later | TSR-041 |
| #18 24h contact response (fake form) | Contradicted | **Remove the promise now** (R1); wire SES delivery as its own fix | TSR-041, TSR-047 |
| #19 Placeholder phone / brand emails | Placeholder | **Remove/replace** with working aliases (no dead `@tsuru.com` addresses) | TSR-041 |
| #20 Newsletter promise | UI shell | **Remove** until a backend exists | TSR-041 |
| #21 "Secure servers / encryption" | Contradicted | **Rewrite to actual practice** (R1 de-claim) + make it true (secrets/authz work) | TSR-041/048; truth: TSR-002, TSR-026..030 |
| #22 GDPR + data-subject rights | Not built | **Remove the claim** (R1); full legal rewrite later | TSR-041, TSR-048 |
| #23 Cookie consent banner | Not built | **Remove the description**; banner only if analytics ever ship | TSR-041, TSR-048 |

Partial/last-mile verdicts (#1 store-in-minutes, #3 WhatsApp selling, #5 QR catalog) convert to Delivered via TSR-080 (+ TSR-086).

## 4. Top-10 gap cross-reference (gap analysis §5)

| Top-10 | Gap | Covered by | Status snapshot |
|---|---|---|---|
| #1 | Close the authorization gap (C1/C7/O1) | TSR-021/022/026/027 (Express — **implemented this session**), TSR-029, TSR-030, TSR-007, TSR-031 | Express side code-complete; gateway userId↔sub, lambda-authorizer, and Python services still open |
| #2 | Fiscal credential + realm hardening (C4/O6) | TSR-002, TSR-003 | **Not started — no workstream owner (§6)** |
| #3 | Unify fiscal sources of truth (C5) | TSR-004 | Not started — no workstream owner (§6) |
| #4 | Unblock the cashier loop (C3/O2) | TSR-005 | Not started |
| #5 | Fix funnel + trust breaches (C6/O4) | TSR-041, TSR-046, TSR-047 | Planned (rebrand R1) |
| #6 | Decide the product thesis | **DECIDED 2026-06-11**: POS/e-invoicing core + honest staged community layer, fairs/trueque built for real | Resolved — encoded in the 4 workstreams + this doc |
| #7 | Repair/de-scope storefront commerce (C2/O5) | TSR-080, TSR-081, TSR-082 | Not started (decision residue on TSR-081) |
| #8 | Finish repo split + retire duplicate surfaces (T1/T2/T4/O7/O10) | TSR-090..094 | In progress |
| #9 | Shared-DB contract hardening (T3) | TSR-032 (+ fairs-be born compliant via arch §3.1) | Not started |
| #10 | Tsuru rebrand + first community surface (S6/S4/O8/O9) | TSR-040 (Done) → TSR-041..043; TSR-069/TSR-060.. | Spec done; execution planned |

---

## 5. Phase plan (Now / Next / Later) and dependencies

### Dependency spine (load-bearing orderings)

```
RBAC code (TSR-020..025, DONE) ─▶ migrate+seed (TSR-026) ─▶ enforce flip (TSR-027) ─▶ Python-side enforcement (TSR-030/031)
                              └─▶ platform_admin gate + BE-connected admin pattern (TSR-025)
                                       └────────────────▶ fairs admin "Ferias (online)" (TSR-063)
fairs-be skeleton (TSR-062) ─▶ admin vertical (TSR-063) ─▶ merchant vertical (TSR-064) ─▶ public SPA (TSR-065) ─▶ trueque (TSR-066) ─▶ launch (TSR-067)
rebrand R1 content (TSR-041) ───────────────────────────────────────────────────────────────────────▶ fairs public launch messaging (TSR-067)
API GW authorizer check (TSR-028) ─▶ any hosted landing-admin build (TSR-025 hosted, TSR-063)
userId↔sub gateway mapping (TSR-029) ─▶ production trust in the whole O1–O15 surface
repo split (TSR-090) ─▶ clean homes for POS/landing/fairs work; mirror step (TSR-100) interim
```

Key dependency statements:
- **RBAC `platform_admin` gates the Tsuru admin.** The landing admin's BE-connected sections (RBAC today, fairs tomorrow) are only safe behind `requirePlatformAdmin` + the gateway authorizer (TSR-026/028 before any hosted admin build).
- **The admin BE-connection pattern (TSR-025) gates the fairs admin (TSR-063).** TSR-025 established the layering (admin-api → repository → service hooks → gated pages, `online:true` manifest rows, tree-shake intact) that fairs admin pages replicate against `VITE_FAIRS_API_URL`.
- **Rebrand content (TSR-041) gates fairs public launch messaging (TSR-067).** The `/ferias` page must already be honest-roadmap-framed so the launch flip is "en construcción → live", never "false present tense → live".
- **Fairs reuses POS theme + catalog plumbing** (theme registry from `themes.ts`, product picker over the shared `products` table) — coordinate with the storefront collapse (TSR-083) so the 28-token registry stays single-sourced.
- **Trueque (TSR-066) is inside fairs**, after the merchant + public verticals exist (offers need stands and a floor).

### NOW (0–3 months) — finish what this session started; stop contradicting

| Order | Items | Notes |
|---|---|---|
| 1 | ~~TSR-026 + TSR-037~~ Done (live DB on the new catalog as of 2026-06-11) | RBAC rollout step 1 complete; next DB-facing step is the enforce flip (row 7). |
| 2 | TSR-028 (API GW /api/admin authorizer verify), TSR-029 (userId↔sub mapping) | Security preconditions for trusting the new surfaces in production. |
| 3 | TSR-041/042/043 (rebrand execution R1→R3), TSR-046 | Content-first; the DXP was built for this. |
| 4 | TSR-005, TSR-006, TSR-008, TSR-011, TSR-012 (POS quick wins — mostly unwired existing code) | Gap analysis O2/O3; S-effort. |
| 5 | TSR-002, TSR-003 (fiscal secrets + realm) | Top-10 #2 — currently UNOWNED; assign before anything else ships publicly (§6). |
| 6 | TSR-062, TSR-063 (fairs M1–M2) | Can start in parallel once TSR-026 lands (admin pattern proven). |
| 7 | TSR-027 (enforce flip; TSR-035 already superseded by TSR-037) | After the catalog reseed + POS roles UI in real use. |
| 8 | TSR-100 (mirror to standalone repos) | Continuous during the split limbo. |

### NEXT (3–9 months) — one product, one truth, one brand

| Items | Notes |
|---|---|
| TSR-064, TSR-065, TSR-066, TSR-067 (fairs M3–M6 + launch) | The flagship community feature, launched honestly. |
| TSR-004 (unify fiscal truth), TSR-007 (x-user-id), TSR-030/031 (authorization on the Python services) | Top-10 #1/#3 completion. |
| TSR-080, TSR-082 (storefront last mile + real publish), decide TSR-081 | Converts Partial promises to Delivered. |
| TSR-090, TSR-091, TSR-092, TSR-093, TSR-094 (split + consolidation: one admin, one provisioner, purified cross-app-be, markets-api cleanup) | Strat §5.1–5.6. |
| TSR-032 (shared-DB hardening), TSR-096 (merge shared libs) | Governance debt. |
| TSR-083 (collapse storefront apps), TSR-095 (data-services) | Shrink work. |
| TSR-047, TSR-048 (contact delivery, legal rewrite) | Trust follow-through. |

### LATER (9–24 months) — community layer depth (gated on fairs v1 traction)

| Items | Notes |
|---|---|
| TSR-068 (buyer/community identity), TSR-073 (story/provenance + org badges), TSR-074 (bundles, crowdfunding, co-op governance) | Vision §5 LATER; each only after its actors/prereqs exist. |
| TSR-084 (publish solidarity pricing before Stripe), then any monetization | Detonation guard for the "gratis" promise. |
| ~~TSR-044 (domain/infra rename wave)~~ **Done 2026-06-20 (TSR-114)**, TSR-045 (Tier 2 cultural identity, RIBCA-gated) | TSR-044 was pulled forward by the PACIFIC-PROD account migration; TSR-045 stays RIBCA-gated. |
| TSR-071 revisit (mutual credit), TSR-087 revisit (second country), TSR-103 (mobile/push) | Only with concrete drivers. |

---

## 6. Open decisions & risks no workstream covers yet (anti-silent-drop list)

Carried from the audit; **none of the four 2026-06-11 workstreams owns these.** Review this section on every roadmap update.

| # | Risk / decision | Severity | Tracked as | Why it can't wait silently |
|---|---|---|---|---|
| R1 | **Plaintext fiscal credentials** (P12 certs, PINs, ATV passwords in Postgres, returned by GET) + staging-hardcoded Hacienda realm | Critical (legal) | TSR-002, TSR-003 | Combined with the remaining authz gaps, any authenticated user could read another org's tax-authority credentials. Top-10 #2 and still unowned. |
| R2 | **Duplicated fiscal truth** — two consecutive allocators, two sale entities, three tax engines synchronized by convention | Critical (legal) | TSR-004 | Drift produces legally wrong invoices. Top-10 #3, unowned. |
| R3 | **markets-api gateway lacks the documented userId↔sub check** — the new RBAC `attachUserId` trusts a gateway validation that doesn't exist yet | Critical (security) | TSR-029 | The entire O1–O15 surface assumes it in production. Cheap (gateway template change) but unassigned. |
| R4 | **Authorization on the Python services** (sales-api/orders-api/data-api have no membership checks; lambda-authorizer TODO stub) | Critical (security) | TSR-030, TSR-031 | Explicitly out of scope of the Express RBAC contract; fiscal documents + ATV credentials are the exposed data. |
| R5 | **Consumer-order build-vs-relabel decision** (promise #6) | High (product) | TSR-081 | R1 fixes the copy; the product decision is still open. |
| R6 | **Monetization narrative before Stripe** | High (brand) | TSR-084 | Activating the dormant Stripe scaffold without the published solidarity pricing model detonates the 4× "gratis, sin cargos ocultos" promise. |
| R7 | **Cross-docking org fabrication** (`_sync_organization`, `owner_id='system'`) keeps `organizations` dual-mastered until TSR-093 | High (data integrity) | TSR-093 | Every new fairs/RBAC feature that reads `organizations` inherits the polluted rows. |
| R8 | **Hosted landing-admin exposure**: the admin shell has no auth of its own; safe only as dev-only/tree-shaken or a PRIVATE build (`VITE_ENABLE_ADMIN=true` never in the public Pages workflow) | High (security) | TSR-025 follow-ups, TSR-028 | The RBAC pages enforce server-side, but a leaked hosted admin build would still expose the shell + other dev-only sections. |
| R9 | **Fairs spec open questions** (spec §13): public hostname timing; one-side vs two-side barter "completed" confirmation; per-fair vs global category taxonomy; stand slug collision policy | Medium | TSR-062 inputs | Must be answered in the fairs M1 implementation workflow; defaults are written in the spec. |
| R10 | **Repo-split limbo divergence** — every monorepo edit (including this session's POS + landing changes) risks silent drift until TSR-090/TSR-100 | Medium | TSR-090, TSR-100 | Guardrail is a CLAUDE.md convention only. |
| R11 | **Drizzle journal anomaly** (`0000_swift_prodigy` without a .sql file) can make `db:migrate` error | Medium (ops) | TSR-102 | Workaround documented (§7 step 1); reconcile properly before the next generated migration. |
| R12 | ~~System `admin` role template can't reach the roles UI under enforce mode~~ **Resolved by TSR-037** — reseed executed 2026-06-11; verified the admin template holds 26 grants incl. the `admin` module | Closed | TSR-035/TSR-037 | TSR-027 unblocked. |
| R13 | **Tier 2 Bribri cultural assets** must stay blocked without RIBCA partnership — a future contributor could "helpfully" ship the cacao logo | Medium (ethics/brand) | TSR-045 | The gate is documented in the rebrand plan §1.3; this row keeps it visible. |
| R14 | **Email-verification + invitation UX holes** (decline no-op, dropped redirects) erode the funnel quietly | Low-Med | TSR-012 | |

## 7. Pending manual steps (updated 2026-06-11 after the deployment session)

The earlier list (migrate, seed, **catalog reseed**, commit, mirror, swagger) is **done**: migration + seeds + org-modules backfill applied; the TSR-037 catalog reseed executed and verified against the live DB on 2026-06-11 (new 6-module catalog live); everything committed and pushed (monorepo `develop`→`main` AND the three standalone repos); the GH Actions Lambda deploy ran green. What actually remains, in order:

1. ~~**Disable/remove the obsolete monorepo CodePipeline stages + buildspecs**~~ — DONE 2026-06-20 (TSR-114/TSR-090): the CodePipeline template, pipeline-roles, deploy-pipeline scripts and all 8 root buildspecs were `git rm`'d (commit `001d965`). Every repo deploys via its own GitHub Actions now.
2. **Request SES production access** (account `947999370977`, us-east-1) — the account is in the SES **sandbox**, so Cognito verification/branded emails (TSR-115/116) only reach **verified** recipients. Until then, test with a verified address (e.g. `chepelcr@outlook.com`).
2. **API Gateway authorizer verify (TSR-028)**: confirm `/api/admin/*` and the org-scoped `/rbac/*` routes ride the proxy WITH the Cognito authorizer before any hosted landing-admin build.
3. **userId↔JWT-sub mapping at the gateway (TSR-029)** — load-bearing for trusting O1–O15 in production.
4. **Enforce flip (TSR-027)**: after real use of the POS roles UI, set `RBAC_ENFORCEMENT=enforce`; consider making POS `usePermissions` fail-closed at the same time. TSR-035 is already satisfied by the realigned catalog.
5. **Admin Cognito pool (TSR-039 follow-up)**: build the dedicated pool, then remove `ADMIN_AUTH_MODE=open` / `VITE_ADMIN_AUTH_MODE=open`. Never deploy a hosted admin build while `open` is set.
6. **Landing admin env (private builds only)**: `VITE_API_URL`, Cognito vars, `VITE_ENABLE_ADMIN=true` — never in the public GitHub Pages workflow.
7. ~~**Repo-split mirroring (TSR-100)**~~ — ENDED 2026-06-12: TSR-090 untracked the folders; the standalone repos are the only home for POS/landing/server changes. No more mirror commits.
8. ~~**Re-run the `tsuru-sales-be` API Gateway regeneration**~~ — RESOLVED 2026-06-13. Root cause: the `/organizations/{id}/configurations` + `/media` routes (in the `organization-configurations` lambda) 404'd because the GH Actions workflow had drifted into a manual, single-service, **skippable-API** deploy, and the 2026-06-12 run was dispatched with `deploy_api=false` (lambda updated, gateway never regenerated). Deeper cause: the workflow no longer matched the old CodePipeline. **Fix (commit `fc92eab`):** `deploy.yml` rewritten to replicate the CodePipeline 1:1 — auto-trigger on push to `develop`, three sequential stages (`pipeline-build.sh` → `pipeline-update.sh` → `pipeline-api.sh`, all services), API-Gateway regen **always** runs (skip toggle removed). The first push-run (`27470473107`) then failed at build because the all-services build hit a **pre-existing broken `document-pdf-generator`** (wkhtmltopdf install → exit 127), which skipped update+api. Disabled that service in the build/update lists (commit `8bec33f`, same convention as `infrastructure-service-provider`; it's SQS-only, not in the gateway). Re-run `27471310518` went **fully green** — gateway regenerated with `organization-configurations` wired in; live probe `GET /organizations/{id}/media` now returns **401 (auth required), not 404**. **Bugs fixed + verified.** Follow-up: fix `document-pdf-generator`'s Dockerfile wkhtmltopdf step to re-enable it. See memory `sales-be-api-gateway-deploy-gotcha`.

9. ~~**Deploy the template-repos publisher OIDC role (TEMPLATE_UPDATED autoload)**~~ — DONE 2026-07-03 (same session, user-approved): `deploys/deploy-template-publisher-role.sh dev` deployed stack `tsuru-dev-template-repos-publisher-role`; `deploys/deploy-params.sh dev --service infrastructure-service-provider` deployed `tsuru-dev-infrastructure-service-provider-ssm-params` (incl. `sns/organizationEventsTopicArn` — CFN-managed, no manual param). deploy-params.sh migrated: profile PACIFIC-PROD, tsuru-* stack name, default org domain `stores.tsuru.jcampos.dev`. `pipeline-update.sh` re-enabled `infrastructure-service-provider` (stale import-error note) so the provisioner lambda now deploys via CI — the TEMPLATE_UPDATED handler is live.

10. **Deploy the 2026-08-21 bug-sweep backends (TSR-138/139/140)** — code is written and tested, nothing is live yet:
    - ~~**store-be migration**~~ — **APPLIED 2026-08-21** against the shared Supabase DB: `y5a6b7c8d9e0` → `z6b7c8d9e0f1` (`alembic current` now reports the new head). Verified live: `check_branch_type` is gone, 6 catalog rows seeded (3 orgs × `stand`/`restaurant`), and 0 of the 34 existing branches are orphaned from a catalog entry. (The script has exactly **one** head — `x4f5a6b7c8d9` is a merge revision reconciling `canon_prod_jsonb_keys` and `w3e4f5a6b7c8`.)
    - **store-be deploy** (branch `develop`): push so GH Actions rebuilds the lambda **and** regenerates the API Gateway — the new `/branch-types` routes plus a long-missing `POST /organizations/{id}/orders` only exist in the regenerated template. The migration is already in place, so the code deploy is the only remaining step.
    - **management-be** (`be/management-be`, branch `main`): push to deploy the username-collision sync fix.
    - **POS** (`fe/pos-system`, branch `main`): push to deploy the register-page rail/story/select changes.
11. **Pin the Cognito-trigger image platform (TSR-137 follow-up)** — whatever builds `tsuru-cognito-templates-ecr` must pass `--platform linux/amd64` (or the Lambda must move to arm64). The function was repointed by hand this session; the next unqualified `docker build` on Apple Silicon will break sign-up again.

## 8. Changelog

| Date | Change |
| 2026-09-07 (POS verticals by business type; TSR-127, TSR-150…164) | **The POS became polymorphic: an organization declares what kind of business it is and gets the matching feature set.** `organizations.business_type` (9 values) plus two *independent* flags — `is_retail_supplier` and `is_pyme` — which are deliberately **not** type values, because a minisuper can be a registered PYME *and* supply a chain and a single-value column cannot express that. The backend is the writer: `syncBusinessTypeModules` resolves the union of type + flags into `organization_modules`, disabling revoked modules rather than deleting them. Applied to `tsuru/dev`: 18 modules seeded, `quotes` reached all 8 orgs, **0 orgs gained a vertical module** — nothing switches on until someone chooses, so no existing org's UI moved. Two capabilities ship **ungated** on purpose: proformas (any business quotes before it sells) and product-code lookup (scanning is how a POS is used) — the latter rides `commercial/read/products` rather than inventing a gate. `useBusinessType` **fails closed**, unlike `usePermissions().hasModule()` which fails open and auto-grants to owners: right for a permission, wrong for a vertical, since a minisuper owner must not see Mesas. **Three bugs were caught by writing tests and checks rather than by reading code**: `ProductTaxDTO` takes `tax_type_id` with a nested `TaxRateDTO`, not the `tax_code` the first pass guessed; `product.cabys` is a relationship, so a `cabys_code` attribute would have silently written `None` on every imported line; and `categories.id` is a String PK, so a UUID `category_id` FK would have failed on apply. **The pedido manual's Documento card is now absent rather than collapsed** — its fields moved onto the Pedido card and its *Notas* was deleted because `useCartFlow` already merged it into *Comentario*; delivery point became registered-point / receiver-address / structured-cascade with no free-text-only path. The payment "Más" menu turned out to be a **CSS-layer bug**: `.dropdown-menu` sat outside `@layer`, beating every utility at equal specificity, so the portaled menu rendered `absolute` at `z-index:30` behind the drawer. On the owner's instruction the Excel importer now emits structured data too — the discount as **07 Descuento Comercial** (the sheet carries an amount but no type, and unlike 99 it needs no reason; critically, only 01/03 divert IVA to `ImpuestoAsumidoEmisorFabrica`) and taxes copied from the product — after querying the live `discount_types` catalog revealed **10 codes, not the 4** both enums carried. Imported *amounts* are untouched: on a cross-docking order the customer's figures are what you reconcile against. Ticket printing (TSR-127) was built **server-side** after a browser-print version was written and rejected, because two maquetadores would drift and a reprint would stop matching the original. 149 tests (59 store-be, 90 POS). Migrations `aa7b8c9d0e1f`, `ab8c9d0e1f2a`, `ac9d0e1f2a3b` and management-be `0019` applied to dev; `db:reseed-rbac` run with 0 custom roles lost. **Not deployed** — code and dev DB only. |
| 2026-08-31 (POS branch-path contract fix; TSR-149) | **The terminals 422 was a frontend contract bug, not an API bug.** store-be addresses branches by their Hacienda integer `code` (`/branches/{branch_code}/...`), but the POS session-setup screen posted the branch **UUID** into that slot, so `branch_code` failed `int_parsing`. Two sibling calls on the stations page had the same defect (branch update, branch status) — and the status toggle was additionally PATCHing `{status}` at the general branch route, whose `BranchUpdateRequestDTO` has no `status` field; it now uses `/branches/{code}/status`. The trap was `CreateTerminalRequest.branch_id: number` (a branch *code*) shadowing `Branch.branch_id: string` (a UUID); since the branch is already in the path and store-be ignores the extra key, the field and the `branchId` form props were deleted rather than corrected. Four files in `tsuru-pos-system`, `tsc --noEmit` clean. **Not deployed** — committed in the POS repo only. |
| 2026-08-21 (register/terminals/branch-types bug sweep; TSR-137…141) | **Two independent causes behind "500 on register", both found in CloudWatch.** (1) The Cognito `CustomMessage` trigger Lambda was dead — configured x86_64 but pinned to an arm64 image, failing `Runtime.InvalidEntrypoint` on every invoke since 2026-06-20; repointed to the amd64 `:latest` image already sitting in ECR and smoke-invoked green (**TSR-137**, live). (2) The Cognito→DB sync re-linked only by email, so a colliding `preferred_username` raised `users_username_unique` and surfaced as a 500 on the profile fetch that runs right after verification; the sync now picks the next free username variant and unique violations map to 409 (**TSR-138**, code only). **`branch-types` was never implemented anywhere** — the gateway's 403 "Missing Authentication Token" was an unknown-route answer, not a broken one — so the per-org catalog was built end to end in store-be: table, migration (which also drops the legacy `check_branch_type` constraint and seeds every org), immutable slug `code`, in-use-guarded delete, 15 tests, regenerated API-Gateway template (**TSR-139**). Fixed `create_terminal` passing 3 args to a 4-arg service (**TSR-140**). On the POS register page, the shared `Stepper` gained `stackedRail` so the 3-step rail keeps its two-row grid beside the brand panel instead of wrapping with a dangling connector; story copy justified; the gender "back to select" control made single-click reliable; native `select` option colors pinned to the app palette (**TSR-141**). Regenerating store-be's swagger also revealed `POST /api/organizations/{id}/orders` had never been wired into API Gateway — it is included now. The store-be migration was applied and verified the same day (head `z6b7c8d9e0f1`; constraint dropped, 6 rows seeded across 3 orgs, no orphaned branches). Backend/FE **deploys** remain pending — all four repos are committed but unpushed. |
| 2026-08-07 (cross-docking detail correction + locale namespaces; TSR-135) | Restored the cross-docking PDF and order PDF actions omitted from the first native-dialog pass, preserving all five explicit downloads without reinstating the iframe. Renamed sale points to “Cajas” and shipped containers to “Bultos” (including “Unidades por bulto”), centered all summary metrics, and changed the article metric to count unique product codes (so two products across 13 cajas display 2, not repeated distribution rows). Split the 4,500-line `LanguageContext` catalog into 13 matching domain JSON files per language (`common`, `orders`, `pos`, etc.); consolidated 109 generic duplicate keys into canonical common labels and added tests for namespace/key parity, accidental common-copy duplication, and missing literal `t()` keys. |
| 2026-08-07 (POS style-system migration; TSR-136) | Completed the repository-wide migration of static inline and legacy runtime-injected styles. Shared responsive grids, POS workspace geometry, collapsible sections, route/item animations, document accents, and responsive header/session rules now live in the central stylesheet; editor/avatar/report color defaults live in theme modules. Removed all JSX `<style>` blocks and direct DOM hover styling. Added `pnpm check:styles` to the normal TypeScript check so static inline declarations and owned hex literals outside the theme layer fail CI/local verification. Dynamic computed geometry and user-controlled visual data remain supported. Verification: style policy, TypeScript, 10/10 tests, production build, and whitespace check pass. |
| 2026-08-07 (native POS cross-docking detail; TSR-135) | Replaced the cross-docking PDF.js preview with a native, responsive distribution view modeled on orders-be's HTML report. The POS now consumes the actual backend field names, shows every item for every sale point, recomputes displayed boxes/units/missing totals from those rows, and avoids the redundant/incomplete fixed-label and “nuevo reporte” PDF tail. Sale-point headers and total footers use the order's persisted green/orange/blue/alternate-green palette. The report palette is centralized in the theme layer, applied once with CSS variables, and consumed by shared report classes; the repeated order-detail grid moved to a responsive design-system class. Opening the view no longer downloads the remote PDF/viewer; Excel exports remain explicit user actions. Verification: POS TypeScript check, 10/10 tests, and production build pass. |
| 2026-08-07 (cross-docking upload CDN + backend CORS; TSR-106/134) | **Corrected and deployed the upload domain end to end in PACIFIC-PROD.** The live `crossdocking-s3-stack` had rolled back a July rename because `uploads.tsuru.jcampos.dev` inherited an incompatible CAA chain from the `tsuru.jcampos.dev` GitHub Pages CNAME. Added an exact-domain Amazon CAA record before ACM validation, then updated the existing CloudFront distribution in place (no bucket/distribution replacement), replaced its certificate and Route53 alias, corrected S3 CORS, and added retain policies for `tsuru-uploads-dev`. Updated cd-backend SSM from stale distribution `E3VMEB1CL8LNEC` to active `E3SNH6UU8M0M3F`; PDF domain remains `https://uploads.tsuru.jcampos.dev`. Explicitly allowed that origin across management/data/sales/store backend sources and pushed all four deployment branches. Live proof: existing cross-docking PDF returns HTTP 200 via the new hostname with `Access-Control-Allow-Origin: https://app.tsuru.jcampos.dev`; data-api preflight from the upload origin returns 200. |
| 2026-08-06 (POS drawer, checkout, and bundle reliability audit; TSR-006/008/011/097/119/130–133) | **Implemented the POS reliability pass across FE and sales-be.** All shared drawers now portal to `document.body` and participate in a stack-aware overlay manager (nested body lock, topmost Escape/backdrop, focus trap/restoration, dialog semantics, `100dvh`, localized close labels); shared/full-screen document modals and mobile drawers follow the same pattern (**TSR-132**). Checkout now selects an active registered economic activity (**TSR-006**), preserves the confirmed/queued receipt until the cashier starts a new sale, snapshots totals before cart clearing, and leaves inventory untouched on permanent rejection (**TSR-131**). Offline sales replay moved from the unauthenticated service worker into the signed-in app, with Dexie queue states/retry metadata and fresh Cognito auth; immediate/replayed requests share an idempotency key, backed by an org-scoped sales-be unique key + advisory transaction lock (**TSR-130**). Removed password persistence in sessionStorage via Amplify auto-sign-in (**TSR-008**), removed sensitive console payload logs (**TSR-011**), and completed the known document i18n leaks/dead keys (**TSR-119**). Added Vitest/jsdom plus 8 passing drawer/replay/permission-loader regression tests (**TSR-097 in progress**). Route-level lazy loading plus permission-before-import gating reduced the main chunk from ~1.49 MB / ~365 KB gzip to 475.21 KB / 129.08 KB gzip, with document editor and QR code support isolated and protected page chunks absent from entry preloads (**TSR-133**). Verification: POS `pnpm check`, 8/8 tests, and production build pass without the prior chunk warning; Python compileall passes. |
| 2026-07-09 (dev-env wiring check + POS FE QA analysis; TSR-119…123) | **Dev environment fully wired per `docs/DEV_ENVIRONMENT_SETUP.md`:** cloned the missing `Infrastructure/` (`chepelcr/tsuru-infrastructure`) and all 8 template repos into `templates/` (incl. `tsuru-demo`, the renamed jmarkets-demo); verified all 16 working copies are on their correct branches, in sync with remote HEAD, and non-empty (`be/store-be` correctly on `develop` — 54 ahead of a stub `main`; POS `migration/dashboard-into-pos` branch confirmed stale, `main` is current). Fixed stale refs: `.gitignore` gained `templates/tsuru-demo/`; setup guide clone loop `jmarkets-demo`→`tsuru-demo` + Infrastructure repo named. **POS FE QA/PO analysis authored (TSR-123):** `docs/qa/POS_FE_QA_ANALYSIS.md` + `POS_FE_QA_ANALYSIS.xlsx` (19 sheets: Resumen + 17 módulos + Hallazgos; generator `docs/qa/generate_pos_qa_xlsx.py`) — full functional inventory of `fe/pos-system` at `65f2ca9` with estado/incidencia/bugs/prioridad per module, cross-referenced to the TSR board. Net-new findings registered: **TSR-119** (hardcoded-Spanish i18n leaks in documents components), **TSR-120** (orphaned unrouted pages AnalyticsPage/AssignmentsPage/`pages/pos/*`), **TSR-121** (stale `.env.example` + npm README in pnpm repo), **TSR-122** (cart shared across document tabs). Root `CLAUDE.md` gained the QA-doc upkeep rule (md changes ⇒ regenerate xlsx same session). **Deep-dive addendum (same session):** fiscal-engine + data-api catalog audit added as two new sheets/sections (Motor-Fiscal: all 10 tax type codes incl. ISEBEC CABYS-branching, IVACE manual base, IVARBU factor, factory-assumed routing, sequential discount cascade 01/02/03/99; Catálogos-DataApi: ~45 catalog hooks, exchange-rate/doc-version contexts, 24h/7d cache with no live invalidation feed). Three more net-new TSRs from `CALCULATION_AUDIT.md` cross-check: **TSR-124** (Exoneración v4.4 has no UI; exemption hooks unused), **TSR-125** (OtherCharges block absent FE+BE; Σ MedioPago reconciliation unenforced), **TSR-126** (ReferencesSection needs Nota 10/10.1 audit). Also spotted: `useAllEconomicActivities` already exists unused — it is exactly what TSR-006 needs. **Shell sweep (same session):** cross-cutting features audited into a Shell-Transversales sheet (sidebar/RBAC nav, notifications bell, dark mode, ES/EN toggle, offline Dexie sync, CSV/PDF export, pagination, session/token) → two more findings: **TSR-127** (receipt printing unreachable — `PRINT_RECEIPT.md` points at a deleted `POSPage.tsx`; checkout `Receipt.tsx` has no print) and **TSR-128** (notifications 100% client-side, no BE feed/persistence — also blocks the catalog-invalidation transport). **Orders deep pass (same session):** Pedidos/Confirmaciones expanded from 3 to 14 rows (status machine 1–5, advanced filters w/ compound `search`, Excel import, reprocess+report colors, type-73 cross-docking upload/summaries/PDF preview, confirmation CRUD, delivery emails) — no new TSRs, all unverified contracts stay under TSR-013. **Sales-subflows + org-deploy pass (same session):** POS-Documentos expanded with the 6 doc types (FE/TE/NC/ND/FC/FExp + per-type RBAC), the 5 checkout sections (document/payments 01-04/06/99/receiver/references/copies), document multi-currency, and the offline-sale queue; CMS sheet now distinguishes the POS publish pipeline from the live org-site provisioner. Two more findings: **TSR-129** (two unreconciled deploy pipelines — markets-api one never exercised, provisioner one has no POS UI) and **TSR-130 (confirmed bug)** — offline-sale replay sends `Bearer undefined` because `useCartFlow` never persists the token `sw.js` expects: **offline sales queue forever and never sync** while inventory is already decremented. Doc totals: 103 functionalities / 22 sheets, findings TSR-119…130. |
| 2026-07-03 (autoload infra live + tsuru-demo transformation + landing rich text) | **Second batch of the session.** (1) **Autoload infra deployed** (user-approved): new `deploys/deploy-template-publisher-role.sh` deployed the `tsuru-dev-template-repos-publisher` OIDC role; `deploy-params.sh` migrated (PACIFIC-PROD profile, `tsuru-*` stack, `stores.tsuru.jcampos.dev` default) and deployed the provisioner SSM params stack incl. `sns/organizationEventsTopicArn` (CFN-managed, replaces the manual-param ask); `pipeline-update.sh` re-enabled `infrastructure-service-provider` (stale disable note) so the TEMPLATE_UPDATED handler went live via CI. End-to-end verified: template deploy → notify job OIDC-assumes + publishes → SNS→SQS→lambda fan-out. (2) **jmarkets-demo → tsuru-demo**: repo renamed `template-tsuru-demo`, working copy `templates/tsuru-demo`, CNAME file + Route53 `tsuru-demo.examples.tsuru.jcampos.dev` + GH Pages cname updated. Full rebrand as the Tsuru example store **in Bribri** (tsuru' = cacao, the brand origin): all UI strings translated (`BRIBRI.md` glossary with confidence flags — **needs native-speaker review**), landing palette (cacao/terracotta/forest/cream), and management-be **migration 0018** (applied to dev) rewrote the template DB row (name `tsuru-demo`, display "Tsuru Ù") + all pages/sections/categories/5 products/theme in Bribri — template UUID unchanged so deployed config.json keys keep working. Monorepo package.json scripts + CLAUDE.md updated (old `jmarkets-demo.examples` DNS record left in place, prune when convenient). (3) **Landing rich text done properly**: `rich-text.tsx` gained `\n\n` spaced-paragraph blocks; ALL public paragraph fields now render through `<RichText>` (Landing/About/Comunidad/Ferias/Funcionalidades/Contact/Cookies/Blog chrome + card descriptions) — previously tokens/breaks rendered raw everywhere except BlogDetail, so the admin's rich-text hint did nothing; blog articles upgraded to spaced paragraphs. Verified on the built bundle (vite preview + headless chromium, 0 errors). |
| 2026-07-03 (template sites fixed: QueryClient + CORS; pnpm frozen; TEMPLATE_UPDATED autoload) | **Diagnosed and fixed why the 8 example template sites weren't loading** (headless-browser console capture on all 8). Two distinct bugs: (1) **white screen** on gourmet-foods/pet-care/tech-gadgets/vintage-fashion — SDK adoption left NO `QueryClientProvider` mounted (`No QueryClient set` crash at boot) + a stray `);` rendered as literal text in 3 App.tsx → provider wired in `main.tsx` (artisan-crafts pattern), strays removed; (2) **stuck loader / dead data** on the other 4 — actual (non-preflight) public-api responses had no `Access-Control-Allow-Origin`: management-be's Express allowlist only knew `*.j-markets.jcampos.dev` → added `*.tsuru.jcampos.dev` regex (`a0edf70`, deployed green); same guard added in **all** BE services per review: data-be `jmarkets_common` + sales-be `jbiller_common` (env-based lists now also carry `allow_origin_regex=^https://([a-z0-9-]+\.)*jcampos\.dev$`) and store-be (wildcard kept + regex). **pnpm properly finished** in all 8 template repos: new `lockfile.yml` workflow (CI has `packages:read` the local token lacks) refreshed `pnpm-lock.yaml` with the SDK resolved, deploys flipped `--no-frozen-lockfile`→`--frozen-lockfile`. **Org autoload built (TSR-118 W2 follow-up)**: each template deploy.yml now publishes `TEMPLATE_UPDATED {templateId}` to `tsuru-dev-organization-events` (OIDC role via repo vars, `continue-on-error` until the role exists); provisioner routes the event → `redeploy_template_to_organizations()` re-mirrors the pre-built bundle into every org bucket whose `config.json` matches + CloudFront invalidation; manual `POST /templates/{id}/redeploy` endpoint added (sales-be develop). Two latent crashes surfaced once data flowed and were fixed: jmarkets-demo `ProductCard` used `useTheme` without importing it (ReferenceError), tech-gadgets `HomePage` called `useProducts` below the `pageLoading` early return (React #300 hooks-order). **Final headless-browser verification: ALL 8 example sites PASS** (render content, zero console errors, signed public-api reads succeed). **Pending manual: §7 step 9** (publisher role stack + SSM topic-ARN param — IAM writes blocked in-session). |
| 2026-07-03 (fix botched domain migration + stop template dual-tracking) | **Cleaned up the monorepo's long-standing uncommitted WIP.** Analysis showed the ~130 pending changes were a **botched global `jcampos→tsuru` find-replace** that corrupted every domain (`markets-api.jcampos.dev`→`markets-api.tsuru.dev`, `tsuru.jcampos.dev`→`tsuru.tsuru.dev`, `@jcampos.dev`→`@tsuru.dev`; 0 correct domains) across docs/deploy-scripts/cloudformation/`fe/dashboard` code. **Repaired to the correct domains** (`*.tsuru.dev`/`tsuru.tsuru.dev`→`*.tsuru.jcampos.dev`, `markets-api`→`api`, `www`/`j-markets` landing host→`tsuru.jcampos.dev`, emails→`@tsuru.jcampos.dev`) across **94 tracked files**, preserving all non-domain content (brand text, `/tsuru` SSM paths, stack renames); local/artifact files (`.claude/*`, `.aws-sam/build.toml`) left uncommitted (`0b91b1b`). **Stopped dual-tracking** the 8 storefront templates: `git rm -r --cached` removed **416** `templates/{8}/` files from the monorepo index (working copies stay on disk, gitignored) so the split `chepelcr/template-*` repos are the sole source — `fe/landing` + `be/*` were already untracked. |
| 2026-07-03 (repo hygiene + dev-setup guide) | **Housekeeping after the SDK/org-site work.** Confirmed org sites key data access by the **org UUID** (in `config.json` `orgId` + every `/api/(public/)organizations/{uuid}/…` call) — the slug is only the human-readable subdomain + S3 bucket name, so no change needed for the "use UUID not slug" security ask. Landing: fixed the one stale `j-markets` URL (`admin/TemplatesPage.tsx` store link → `{name}.examples.tsuru.jcampos.dev`, matching `Examples.tsx`) and relaunched the deploy (green; `tsuru.jcampos.dev` 200). Relocated the SDK working copy into the monorepo tree at **`fe/storefront-sdk`** and gitignored it (matching the `fe/landing`, `be/*`, `templates/*` split-repo pattern). Pushed pending split-repo WIP: **sales-be** `/jcampos→/tsuru` SSM-path migration (settings.cfg across services + deploy-params + docs, `6abad2e`); **store-be** gitignored the local `urgent/` scratch dir (`044eada`). Added **`docs/DEV_ENVIRONMENT_SETUP.md`** — a full guide to stand up the ecosystem on a new machine (repo/clone layout, AWS/Cognito/DB/SSM pointers, GitHub-Packages auth for the SDK, per-component config, deploy paths). |
| 2026-07-03 (all 8 templates on the SDK + fruta-fresca & modas-laura org sites live) | **Rolled the storefront SDK out to all 8 template repos and stood up two live org sites.** (1) **All 8 template repos** (`template-{gourmet-foods,artisan-crafts,beauty-essentials,fitness-hub,jmarkets-demo,pet-care,tech-gadgets,vintage-fashion}`) now consume `@chepelcr/tsuru-storefront-sdk`: `main.tsx` configures Amplify (guest pool), `api.ts`/`locations-api.ts`/`SubdomainContext` delegate to the SDK, demo `config.json` carries the template UUID, deploy.yml gets the identity-pool env + `packages: read` + `NODE_AUTH_TOKEN` + `--no-frozen-lockfile`. All 8 example sites rebuilt + deployed green (verified serving the signed SDK bundle: `AWS4-HMAC`/`public-api`/UUID config). The 3 identical infra files were copied from the proven gourmet-foods; `main.tsx`/`package.json` patched per-repo (beauty-essentials' old `./lib/amplify` init removed). GH-Pages "try again later" transients cleared on rerun. (2) **fruta-fresca org site** — `applyTemplate` cloned gourmet-foods content (theme/pages/categories; products stay demo-only) into the org via a direct lambda-invoke of `PUT /api/organizations/{orgId}/template`, then re-provisioned (had to reset `infrastructure_status` off `active` — the provisioner is idempotent on active) → **`https://fruta-fresca.stores.tsuru.jcampos.dev`** live on the SDK bundle. (3) **modas-laura org site** — published its `ORGANIZATION_REGISTERED` event (templateId `vintage-fashion`) → provisioner mirrored the vintage-fashion SDK example + injected live config → **`https://modas-laura.stores.tsuru.jcampos.dev`** live (200), signs guest requests to the org endpoints; modas-laura's real theme/pages/products/categories all return 200. Commits: gourmet-foods `3fdf8b0`, the other 7 template repos, mgmt-be applyTemplate invoke (no code). |
| 2026-07-03 (storefront SDK + template_products full-match + demo federation) | **Standardized the storefront data layer into a shared package + made the demo catalog consistent with real products.** (1) **`@chepelcr/tsuru-storefront-sdk`** — new **public** repo published to **GitHub Packages** (tag-triggered `publish.yml`, `GITHUB_TOKEN`): owns `/config.json` mode resolution, W7 **guest SigV4 signing** (Amplify guest creds + `aws4fetch`), the demo-vs-live URL client (demo→`/api/templates/{id}/…`, live→`/api/public/organizations/{orgId}/…` + `/api/organizations/{orgId}/…` + `/countries/…`), and canonical `Storefront*` DTOs normalizing store-be snake_case ⟷ management-be camelCase. Signing verified 200 vs the live public API. So the "correct pattern" lives in one place — future fixes are a version bump, not an 8-repo sweep. (2) **`template_products` full-match** — was a 19-col stripped shape; management-be migration `0017` brings it to an exact 40-col mirror of store-be `products` (status/created_on/deleted_on + all Hacienda/fiscal fields + is_offer/low_stock_threshold/track_inventory/units_per_box; drop is_active/sku), entity+seed updated, applied to dev (0 missing/0 extra vs products). Template products stay **demo-only** (NOT copied to orgs — only template categories clone on org create). (3) **Public API federation** — added the per-resource template routes (`/api/templates/{id}/theme|contact|pages|pages/{slug}|categories|products`) so demo reads go through the guest API; regenerated + deployed `tsuru-dev-public-api` (26 paths); verified 200 signed with the full product shape. (4) **Proof: `template-gourmet-foods` adopted the SDK and deployed** — its live bundle now guest-signs to public-api (`AWS4-HMAC`/`GetCredentialsForIdentity`/`execute-api`), config.json uses the template UUID. Gotcha: template deploy workflows need `permissions: packages: read` (else `ERR_PNPM_FETCH_403`) + `NODE_AUTH_TOKEN`. Commits: sdk `993f2b3`+tag v1.0.0, management-be `e4e08f9`, public-api redeploy, gourmet-foods `3fdf8b0`. **Remaining:** replicate the thin SDK adoption to the other 7 template repos + re-provision fruta-fresca (+ applyTemplate for content). |
| 2026-07-03 (TSR-118 W2 org-site provisioner LIVE — first org site up) | **The org-site provisioner (sales-be `infrastructure-service-provider`) is finished, deployed, and provisioned its first live org site end-to-end.** It was never completed for custom domains and relied on an in-Lambda `git clone`+`npm build` the python image can't run. Rewrote it to the correct model: templates are generic Vite SPAs that fetch `/config.json` at runtime and read data from the guest public API by `orgId`, so the compiled bundle is identical across orgs — the provisioner now **mirrors the pre-built template** from `{template}.examples.tsuru.jcampos.dev` into the org's S3 bucket and injects `config.json={mode:"live",orgId,templateId}` (no git/node/npm/PAT). Also finished: CloudFront **Aliases + wildcard ACM cert + SPA 403/404→index** (was serving only `*.cloudfront.net`); threaded the `{subdomain}.stores.tsuru.jcampos.dev` FQDN; `ROUTE53_HOSTED_ZONE_ID`/`DOMAIN_SUFFIX` env; best-effort deploy-tracking (the `organization_template` table doesn't exist → don't abort the mirror); S3 website/CORS best-effort. Infra/deploy fixes: role trimmed to ≤10 managed policies (AWS cap; kept app-config/ssm/secrets/decrypt/sqs/s3/api-gateway-iam[route53+acm]/fe-deploy[cloudfront+bucket]); dropped LogGroup KMS key; added `jbiller_common.DataBaseException`; fixed two "src-as-task-root" import bugs (`from src.models`→`models`, `from ..dtos`→`dtos`). **Test:** created a fake org **fruta-fresca** (`bdf77b43-…`, template `gourmet-foods`) for chepelcr@outlook.com in the DB, published `ORGANIZATION_REGISTERED` to SNS → SQS → Lambda → S3 mirror + CloudFront + Route53; `organization_settings.infrastructure_status=active`; **`https://fruta-fresca.stores.tsuru.jcampos.dev` is live (200, storefront shell)**. Commits sales-be `e80bc02`/`41810a3`/`3be46b3`/`15f94fc`, stack `tsuru-dev-infrastructure-service-provider-lambda`. **Follow-up (not provisioner):** the deployed `examples` bundles predate W1/W4 — they call the authed `api.tsuru.jcampos.dev/organizations/{orgId}` directly, not the guest public-api with SigV4, so a live org site renders the shell but won't load org DATA until the template repos are rebuilt with guest signing (and the org has content). |
| 2026-07-03 (POS data-api URL fix) | **Rollout debugging — POS back-office data-api calls were unreachable.** `fe/pos-system` builds fell back to the stale-brand default `https://data-api.jcampos.dev` (no `tsuru` subdomain → no DNS record → `net::ERR`/000) because the deploy workflow never set `VITE_DATA_API_URL`; this broke exchange-rate, document-versions, and every Hacienda catalog call. Fixed in `chepelcr/tsuru-pos-system` (`65f2ca9`): added `VITE_DATA_API_URL=https://data-api.tsuru.jcampos.dev` to `deploy.yml`; corrected the fallback defaults in `src/services/data-api/client.ts` and `src/lib/api.ts` (api/orders-api/sales-api → `*.tsuru.jcampos.dev`, was `markets-api`/bare `*.jcampos.dev`), `siteUrl.ts` org-store URL → `{subdomain}.stores.tsuru.jcampos.dev`, and `CreateOrganization.tsx` base-domain fallback → `tsuru.jcampos.dev`. **Verified via guest-SigV4 through `public-api.tsuru.jcampos.dev` + direct lambda invoke that all backends are healthy for org `modas-laura`:** management theme/contact/pages, store products/categories, data-be full CR (code **188**) address cascade + exchange-rate (₡456 USD) + document-versions (v4.4) all return 200 with real data — the failures were purely the POS URL config, not the services. Deploy run 28662415723. |
| 2026-06-30 (TSR-118 storefront MVP authored) | **9 of the TSR-118 work-items authored + statically verified via a multi-agent workflow (author-only; owner deploys).** A 5-cluster workflow lost its structured results to a tool failure but its edits survived on disk; salvaged + verified all: **W6** landing harmony + **W13** blog (`fe/landing` `BlogDetail.tsx` + `/blog/:slug` + content JSON; typecheck green); **W10** product types/offer + filters & **W11** anonymous tracked-order `POST /api/organizations/{id}/orders` with structured CR address (`be/store-be`; 2 Alembic migrations; py-compile green); **W4** template API repoint (all 8 → `public-api`/`stores.tsuru.jcampos.dev`); **W12** `programs` template-gated dashboard submodule (`management-be` rbac-seed + POS `ProgramsPage`/hook/route/i18n; typecheck green). The one cluster that produced nothing (Infrastructure) was authored by hand: **W1** combined public API IaC — generator `Infrastructure/public-api/gen_public_api.py` + config + generated `template.yml` (20 public paths → `tsuru-${env}-api-handler` / `cd-backend-${env}-lambda` / `tsuru-${env}-hacienda-locations-lambda`, AWS_IAM/SigV4 + CORS) + samconfig + `deploy.sh` (CAA-then-SAM); **W7** anonymous Cognito identity pool `Infrastructure/cognito/tsuru-public-identity-pool.yml` (guest unauth role scoped to `execute-api:Invoke`). **Pending manual rollout (owner):** DB migrations (W10 cols, W12 rbac reseed), SAM deploys (W7 then W1), repo pushes (landing/store-be/templates/management-be/POS). **Still TODO:** W2 (org-site provisioner re-enable, sales-be) + W4 guest-SigV4 signing in templates (needs W7's identity-pool id). |
| 2026-06-20 (storefront examples online) | **TSR-118 W5+W8 done** — 8 storefront templates split into their public `chepelcr/template-{name}` repos, **converted to pnpm** (lockfile + packageManager + only-allow), each with a GH-Pages workflow; **all live + HTTPS 200 at `{name}.examples.tsuru.jcampos.dev`** (Route53 CNAME→chepelcr.github.io, per-repo Pages cname, LE cert auto-provisioned past the CAA via Let's Encrypt). Repos flipped public (free plan blocks Pages on private). Demo mode via bundled `public/config.json` (same mechanism W2 will use to inject `orgId` for real org sites). Monorepo `.gitignore` split markers added (files still tracked). Pending: one account-level domain-verification TXT for `examples.tsuru.jcampos.dev` (GitHub-generated challenge). `pos-landing`/`pollo-porteno` deferred. |
| 2026-06-20 (PACIFIC-PROD migration + Cognito emails) | **Whole ecosystem migrated to the PACIFIC-PROD account (`947999370977`) + Tsuru-domain cutover — platform back online.** The `J-CAMPOS` account was dead (invalid creds), so everything was stood up fresh in PACIFIC-PROD with `tsuru-*` naming. **New: TSR-114** (account migration umbrella) — Cognito (`tsuru-cognito`, fresh pool `us-east-1_0cTNJ31eo`, users re-onboard), IAM policies/roles (reused the existing account OIDC provider), SAM+uploads buckets, Secrets `tsuru/dev/database`, SNS, SSM `/tsuru/dev/{platform,commondata,hacienda}`; all 4 backends repointed + deployed (`api`/`data-api`/`sales-api`/`orders-api`.`tsuru.jcampos.dev` live, verified); landing `tsuru.jcampos.dev` + POS migrated S3→GitHub Pages `app.tsuru.jcampos.dev`; DNS in zone `Z08953202H0791MTWJHTP`; recycled IaC pruned (backup in `E:/dev`). Gotchas: CAA-under-GitHub-Pages-CNAME → per-subdomain CAA (no wildcard); IAM SSM scope `/tsuru/*`→`/tsuru/*`; deploy-roles `route53:ListHostedZonesByName`; image-Lambda ECR repo policy. **This executed TSR-044** (domains, was deferred) and **finished TSR-090** (CodePipeline IaC deleted, commit `001d965`). **New: TSR-117** returning-user org re-link by email (`c9a964c`). **New: TSR-115** custom Cognito emails — standalone repo `chepelcr/tsuru-cognito-templates` (CustomMessage Lambda + 8 SES templates, pool wired) built from a new reusable **`cognito-custom-emails`** skill. **New: TSR-116** Cognito now sends via SES `tsuru@jcampos.dev`. **TSR-106** CORS updated to the Tsuru frontends (`7ded0c7`; `app.tsuru` was missing). Dashboard deploy intentionally skipped (owner). Carryover: the migration preserved (did not fix) the R1–R4 security debt + TSR-034. §7 step 1 cleared; new §7 step = request SES production access (sandbox). |
| 2026-06-14 (POS → pnpm) | **`tsuru-pos-system` migrated npm → pnpm** (`dd278a3`). The flag-dep deploy failure was root-caused to the repo being npm-based (CI `npm ci` vs a `pnpm add` that only touched `pnpm-lock.yaml`) — fixed short-term by syncing `package-lock.json` (`fcf9baa`), then migrated the repo to pnpm for consistency with `fe/landing`: `deploy.yml` uses `pnpm/action-setup@v4` + `pnpm install --frozen-lockfile` + `pnpm run build`; `package.json` gains `packageManager` pin, `preinstall: only-allow pnpm`, a `check` (tsc) script, and `pnpm.onlyBuiltDependencies: [esbuild]`; committed `pnpm-lock.yaml`, removed `package-lock.json`; CLAUDE.md + deploy.sh updated. CI deploy green (run 27490056164). POS now matches landing's pnpm tooling. |
| 2026-06-13 (shared phone/flag + location consolidation) | **Shared location + phone form components, real flag images, ResidenceStep consolidated.** New shared UI in `tsuru-pos-system`: `CountryFlag` (bundled SVG via `country-flag-icons` — NOT emoji, renders on Windows + offline, keyed by the data-api alpha-2 `iso`), `CountrySelect` (searchable country picker rendering flags — a native `<select>` can't show images), and `PhoneField` (flag country picker + dial code + number). `LocationSelect` was i18n-ized (new `location.*` keys es/en), gained an optional header, and now **hides the CR provincia/cantón/distrito/barrio cascade when the country isn't Costa Rica** (only the free-text address remains). `ResidenceStep` (fiscal residence) collapsed to a thin adapter over `LocationSelect` (bespoke cascade deleted); fiscal `ContactStep`, the org-settings **contact form**, and the **client** contact form all now use the shared `PhoneField` (flag country picker). Contact phone country persists via a new `contact_settings.phone_country_code` column (`tsuru-management-be` migration `0016` + entity + response mapper, applied to dev). tsc + vite build green. Commits: `tsuru-pos-system` `a5065a3`/`2cb2a41`/`c9a31c3`, `tsuru-management-be` `31d31f3`. |
| 2026-06-13 (contact location editing + step2 fix + reuse) | **Org-settings Contact page now edits the structured location; onboarding step2 contact-update bug fixed.** (1) `completeOnboardingStep2` passed the *org id* to `contactSettingsRepo.update()` which filters by the row PK → a re-submitted/resumed step2 silently dropped contact + location edits; fixed to use `existingSettings.id` (`tsuru-management-be` `62b2697`). First-time registration (create path) was already correct — verified the full create flow maps to `contact_settings` with no writes to the dropped org columns. (2) `ContactSettingsForm` now renders the shared `LocationSelect` cascade (seeded from `org.contact`), replacing the plain address textarea (its "otras señas" is the single address field); save merges state/county/district/neighborhood ids + address into `PUT /settings/contact` → upserts to `contact_settings` (`tsuru-pos-system` `cf1b14e`). (3) Registration contact step folded to a single address field via `LocationSelect` too, matching the contact page (`162ccb7`). **Reuse note:** `components/ui/LocationSelect` is the canonical location cascade — now used by clients (AddressSection/ClientDrawerForm/ClientFormBody), stations (BranchLocationSection), registration, and the contact form. Lone outlier: `ResidenceStep` (fiscal residence) still hand-rolls the cascade because `LocationSelect` hardcodes Spanish labels (i18n debt); consolidating it is deferred until `LocationSelect` is i18n-ized. tsc green throughout. |
| 2026-06-13 (RBAC plantilla + legacy drop + location step) | **Three deferred backlog items shipped via an 11-agent file-partitioned workflow (code) + main-loop DB ops.** (1) **RBAC**: `plantilla` is now a proper submodule of the `organization` module (`tsuru-management-be` rbac-seed.ts) — POS dropped the `storefront/templates` special-case in `OrgSettingsPage.sectionVisible`; applied via idempotent `db:seed` on dev (new submodule + 2 actions; module-wide org grants already cover it; `templates` kept under storefront for the sidebar). (2) **Legacy fields dropped**: `organizations.email/phone/address` removed (entity + mapper read-time fallback + update whitelist + swagger); migration `0014` backfills `contact_settings` from the org columns first (no data loss) then drops them — contact now sourced solely from contact_settings. (3) **Structured location**: `contact_settings` gains `state_id/county_id/district_id/neighborhood_id` (migration `0015` + entity + response mapper); POS registration **contact step** now captures structured location via the existing `LocationSelect` (CR catalogs), persisted through the step2 pass-through and round-tripped in the org response. Both migrations applied to dev; tsc green both repos. Pushed: `tsuru-management-be` `a483566`, `tsuru-pos-system` `815585a`. Also fixed mobile org-card overflow (`88f77df`: title `min-w-0 flex-1` so it truncates instead of pushing the grid past the viewport). |
| 2026-06-13 (sidebar/badge fixes + skeleton sweep) | **POS UX polish (`tsuru-pos-system` `33bdc5e`, `24636ac`).** (1) Plantillas page no longer unmarks the sidebar — `getActiveNav` maps the relocated templates route to `organization` so the org-settings item stays highlighted (templates now lives in the org "Plantilla" card, no own sidebar item); dropped a per-nav `console.log`. (2) Org-settings card badges (Configurado/Pendiente) pinned to the right of each title row (justify-between + truncate) so they align across cards. (3) **Skeleton-loader sweep** — replaced "Cargando…" text / bare spinners / spinning icons with layout-mirroring skeletons across drawers + drawer-content (Session detail Overview/Assignments/Sales tabs, ClientSelector, ReceiverPicker, ClientOrderHistory, LineDetailDrawer body, customer-type pills) and 14 pages (Product/Client/Order/Confirmation detail; Categories/Gallery/Deployments lists; OrgGeneral/Branding/Contact/Payment/Shipping/Theme + Templates). Page sweep ran as a 14-agent parallel workflow (one file each, inline skeletons, design-system tokens only); tsc green. Audit found the list/grid pages (Products/Clients/Orders/Confirmations/Dashboard/Documents) + select-cascade and button-spinner loaders were already correct. |
| 2026-06-13 (dedicated template endpoint + Examples cards) | **Apply-template duplicate-key fixed.** Re-applying a storefront template post-onboarding reused the registration-only step3 route (plain inserts) → duplicate-key. Added a dedicated idempotent `PUT /api/users/{u}/organizations/{orgId}/template` (markets-api `tsuru-management-be` `25dd976`): `TemplateCloneService.replaceExisting` clears the org's existing cloned content (sectionContent→pageSections→pages, +categories) before re-clone; `OrganizationService.applyTemplate` updates only `templateId` (not `onboardingStep`); step3 stays register-only. POS (`tsuru-pos-system` `6a67464`): `useApplyTemplate` repointed off step3 to the new PUT. New route → API Gateway regen runs on the management-be push. Landing (`tsuru-landing` `d702206`): Examples cards — title now inline with the icon, "Destacada" badge pinned to the card corner (absolute) so it no longer competes with the category pill. tsc green all three. Pending (next): proper RBAC move templates→organization submodule + reseed (dev), drop legacy org email/phone/address columns, structured-location registration step. |
| 2026-06-13 (org UX + CI + landing) | POS: org sub-pages now hydrate from the embedded org sections (no per-section re-fetch); theme read via GET /configurations/theme (fixes Pendiente + persists across logout via localStorage); theme-page back button + return-to-org on select; templates relocated as the org "Plantilla" card (gated on existing storefront/templates — no reseed). Confirmed: selecting a template at registration auto-stores the theme (onboarding step3 → /configurations/theme). sales-be CI → native-arm parallel matrix (mirror data-be). Landing: cards standardized to the Community layout (Landing/Funcionalidades/Ferias) + Examples skeletons + "Destacada" overlap fixed. Pending (next): proper RBAC move templates→organization submodule + reseed (dev), drop legacy org email/phone/address columns, and a structured-location (country/state/county/district) registration step reusing the fiscal residence data-api. |
|---|---|
| 2026-08-24 (no-USD decision) | **Colones only, both languages — decided; no code change needed.** Confirmed the EN landing never showed USD (`formatCRC` takes no language input, `config.currency: CRC`, EN labels are suffixes like "/ month"), so there was no burned rate on the landing at all. Evaluated wiring `be/data-be/app/consumer-exchange-rate` (Hacienda-sourced, DB-cached) into the page and **decided against it**: Tsuru is only usable by a Hacienda filer so there is no foreign-currency buyer segment, and card networks convert at charge time at the issuer's rate + FX fee — any USD figure printed would be systematically low versus the real statement, i.e. a number we can't stand behind. Verified the endpoint is Cognito-protected (`data-api` → 401, `public-api` → 403), so no public-API exposure is now needed. The service stays where it is load-bearing: the **POS** (`ExchangeRateContext`), where Hacienda requires the BCCR rate on USD invoices. `fe/pos-landing`'s `usdRateCRC: 600` reframed — the CRC\|USD toggle itself should be deleted rather than fed a live rate; **parked**, that site isn't deployed. All `tsuru_pricing_market_research.md` items are now closed. |
| 2026-08-24 (IVA decided) | **Prices are FINAL — IVA included** (`tsuru-landing` `27f4ec1`). Stated beside the amounts under the billing toggle, not buried: for a brand promising "sin cargos ocultos", a 13% surfacing at checkout would be the hidden charge itself. Also answered as a FAQ entry ("¿Los precios incluyen IVA?", placed third). Copy in `planLabels.taxNote`, editable from `/admin/plans`. Recorded in both planning docs; the `plans` table comment now states amounts are **IVA-inclusive gross** so nobody adds tax on top at checkout. Improves the competitive read: **GTI quotes "+ IVA"**, so its real cost is 13% above list (persona física ₡7.650 → ₡8.645; empresa ₡12.750 → ₡14.408). **TSR-084 / TSR-147 now have nothing blocking `draftPricing` being turned off.** |
| 2026-08-24 (colones + price corrections) | **Prices decided and applied** (`tsuru-landing` `db78156`). **Cooperativa ₡45.000 → ₡35.000/mes**, under the ₡36.000 CR market ceiling (Alegra POS Plus / POSMOVI Premium); **annual discount 2 → 3 free months (25%)** → Cosecha **₡180.000/año** (₡15.000/mes efectivo, under Alegra POS Pro's ₡17.000), Cooperativa **₡315.000/año**. Annual stays exactly 9× monthly so "pagás 9 meses y te regalamos 3" is arithmetically true; toggle badge, savings note and Cosecha subline all updated. **Colones-only, CR formatting:** `Intl` `es-CR` groups thousands with a NO-BREAK SPACE (₡200 000, reads as a typo in CR) — new `src/lib/currency.ts` groups explicitly with a period (**₡200.000**), whole colones, no decimals, used by the plans page and home teaser. No USD anywhere on the landing. Market research doc rewritten **colón-first** with decisions recorded. |
| 2026-08-24 (market research + origin fix) | **New: TSR-147/148.** Wrote `docs/roadmap/tsuru_pricing_market_research.md` — CR market check at ₡452/USD. Tsuru competes in **POS + e-invoicing ($20–80/mo)**, not pure invoicing ($5–20): **Cosecha ($44.25/mo, $36.87 annual) is defensible**; **Cooperativa ($99.56/mo) is above the $80 market ceiling** → recommend ₡35.000/₡350.000. Biggest strategic finding: **Hacienda's TicoFactura is free AND uncapped**, so a hard document cap pushes merchants to a government product — which independently confirms the owner's **soft-cap** decision (warn at 80%, never block emission) plus non-expiring **document packs** (₡2.500/25, ₡8.000/100), now spec'd in TSR-145 §5.6 with a `document_packs` table and FIFO consumption. Also flagged: the page never states **IVA** treatment (every competitor does) and `fe/pos-landing`'s `usdRateCRC: 600` is ~33% stale. **Origin corrected** (`tsuru-landing` `6fe138a`): Tsuru was born in **Puntarenas** — Guápiles is where the inspiring org, **Feria del Trueque Verde Manantial**, is; fair references kept, birthplace fixed across branding/about/legacy translation keys. Footer gained a **Pacific Code Labs** attribution (cropped alpha logo, `madeBy` in footer.json, editable in /admin/footer) linking to pacific-code-labs.jcampos.dev. |
| 2026-08-24 (plans compliance + spec) | **Two compliance corrections to the published tiers, plus New: TSR-145/146.** Hacienda requires every taxpayer to issue the full receipt set and to have a contingency path when ATV is down — so **all document types (FE, TE, NC, ND, FC, FEX)** and **modo contingencia ATV** both moved from Cosecha into the free **Semilla** tier, with the reasoning stated in the solidarity promise ("emitir lo que la ley te exige nunca va a ser una función de pago"). Cosecha inherits both via "Todo lo del plan Semilla". Plan **CTAs are now inert "Próximamente"** (`config.ctaComingSoon`, admin-flippable) since nothing is purchasable until TSR-145. Wrote **`docs/roadmap/tsuru_plans_implementation.md`** (539 lines) — the BE/org-creation/POS spec: entitlements as a second axis from RBAC (402 + `requiredPlan`, never 403), three new tables with `NULL`=unlimited and per-org `overrides`, plan activation writing the existing `organization_modules`, `PLAN_ENFORCEMENT=log→enforce` mirroring the RBAC rollout, `useEntitlements()` mirroring `usePermissions()`, fail-**open** on emission (never block invoicing on our own outage), no plan step in onboarding, and a drift test between `plans.json` and the seed. Steps 1–7 deliver working tiers with zero Stripe. Landing verified + deployed (commits `4dea981`, `a2a3a50`). |
| 2026-08-23 (monetization) | **TSR-084 model published + New: TSR-142/143/144.** `templates/pos-landing` → **`fe/pos-landing`** (`git mv`; stays tracked — no standalone repo exists; all build/deploy refs repointed, `dist/pos-landing`, build green). **Landing is now monetizable**: four-tier solidarity model (**Semilla ₡0 forever / Cosecha / Cooperativa / Feria a-convenir**) shipped as a proper DXP entity — `plans.json` + `/planes` (+`/pricing`) + home teaser + `/admin/plans` editor + versions row (four pieces), seeded from `fe/pos-landing`'s real pricing and the landing's mapped functionality. The 4× "gratis" promise is held by an explicit promise block (e-invoicing free on every tier, no sales commission, free export, no expiry); **amounts are preliminary behind `config.draftPricing`**. Also: **logo slot reserved** (`BrandLogo`, navbar+footer, no layout shift when the wordmark lands) and the **Funcionalidades 7-card grid** de-orphaned (3/2/2, derived from card count). Verified: tsc clean on new code (pre-existing `NodeJS.Timeout` error unrelated — `@types/node` absent), vite build green, admin tree-shaken (`__local` + `PlansPage` absent from `dist`), prerender emits `/planes` + `/pricing` with SEO + sitemap, inventory regenerated (138 nodes). QR for the business card in `docs/brand/qr/`. |
| 2026-06-11 | Document created. Encoded the four owner decisions (rebrand spec, fairs spec+architecture, RBAC implemented in Express, trueque MVP in fairs). Marked TSR-020..025 Done per the implementation + verification reports (code complete, 51/51 RBAC tests, builds clean, migration generated-not-applied). Top-10 #6 (product thesis) recorded as DECIDED. |
| 2026-06-12 (untracking) | **TSR-090 untracking executed** — `templates/pos-system` + `landing-client` + `server` removed from the monorepo index (592 files; local folders kept as standalone working copies); obsolete GH Pages landing workflow deleted (duplicate deploy path killed); root CLAUDE.md split rules rewritten; **TSR-100 mirroring ENDED**. Remaining: disable the dead CodePipeline stages/buildspecs (§7 step 1). |
| 2026-06-12 (shell polish) | **New: TSR-111 Done** — sidebar footer shows the org role (not platform `customer`), mobile notifications panel centered (fixed overlay), system-role drawer shows the localized role description. |
| 2026-06-12 (permission sweep) | **New: TSR-110 Done** — workflow-driven full-POS action-gating sweep (133 elements, 6 areas), roles-drawer doc-type picker (nested under Emitidos→Crear), localized RBAC catalog + system-role names ES/EN, confirmations `create` action added to the live catalog. |
| 2026-06-12 (reseed + /pos retirement) | **Catalog reseed EXECUTED and verified** — live DB now has all 7 modules incl. `organization` (9 sections) and the doc-type gates `documents/fe|te|nc|nd|fc|fexp`; staff confirmed restricted to emitted+fe+te (TSR-107/108/109 fully Done; §7 step 1 cleared). POS: standalone `/dashboard/pos` route retired — redirects to `/dashboard/documents`; `DASHBOARD_POS` constant + `pos` NavId removed (POSIntegratedPage stays as the per-tab editor view). |
| 2026-06-11 (doc-type gating) | **New: TSR-109** per-doc-type create permissions (cashier = FE/TE only; submodule-specific grant support in the seed matrix). **TSR-108 migration 0002 APPLIED** to the live DB (General/Theme persistence unblocked). NotificationsDrawer toggles centered on mobile. Remaining manual step: the catalog reseed (§7 step 1). |
| 2026-06-11 (org-settings session) | **New: TSR-107** fine-grained `organization` module (9 submodules = org-settings cards; per-card POS gating + per-section BE guards), **TSR-108** org-settings persistence fixes (org `theme`/general columns + PATCH routes + update whitelist; "theme not persistent" root-caused to missing column/route). Roles drawer PermissionMatrix now an accordion (one module card open at a time). Reusable skill **`rbac-org-modules`** authored at `~/.claude/skills/` (BE/DB/FE + landing platform-admin pattern). §7 step 1 added: migration 0002 + catalog reseed (owner-run). Note: system `admin` template intentionally lacks `delete` on the `admin` module (owner-only destructive ops — `rolePermissionMatrix` comment). |
| 2026-06-11 (reseed) | **TSR-037 fully Done** — owner-triggered catalog reseed executed against the live DB and verified: 6 modules / 18 submodules / 68 submodule_actions / 30 org_modules / 100 role_permissions (module-wide grants); admin template reaches the roles UI (R12 closed); memberships preserved. §7 step 1 removed — remaining manual steps start at the CodePipeline stage disable. |
| 2026-06-13 (loaders + bug triage) | POS UI polish pushed to `main` (`tsuru-pos-system`): app/store loader swapped from the 🏪 emoji splash to the standard `Spinner`; org-settings page loading now shows responsive skeleton cards (mirrors products/clients). **Diagnosed (not yet fixed):** org-configurations `/configurations` + `/media` 404 — the 2026-06-12 sales-be deploy ran with `deploy-api` skipped, so the `sales-api` gateway was never regenerated (lambda updated, routes stale). Remedy recorded in §7 step 8 (needs go-ahead). |
| 2026-06-13 (structured org response + de-dup) | **markets-api org response restructured into per-card nested sections; field duplication removed; both deployed.** `GET /memberships/organizations` (+ /default, user org list, org GET) now return `general` top-level (name/description/identity/system) + nested `contact`/`branding`/`payment`/`shipping` (null when unconfigured), assembled from the settings tables via `src/mappers/organizationResponse.ts` — mirroring the `/configurations` structured shape so each org-settings card consumes its slice. **De-dup:** `contact` owns email/phone/address (read-time fallback to the legacy org-row columns → no DB migration; `PATCH /settings/general` narrowed to name+description). **Dropped** the orphaned `theme` (now in org-configs) + deprecated `settings` jsonb from the response. POS consumes the nested shape and the org-settings **"Pendiente" badges now read the embedded sections** (the long-standing false-pending bug — fixed). No path changes (gateway untouched). Deploy order markets-api→POS, both green; tsc clean both repos. |
| 2026-06-13 (theme relocation + org path standardization) | **Theme moved to org-configs + markets-api paths standardized — both live & verified.** (A) POS UI theme now persists on `organization_settings.theme` in the org-configs lambda (sales-api) via a dedicated `PATCH /organizations/{id}/configurations/theme` (migration `a7b8c9d0e1f2` applied to dev DB; new DTOs + repo upsert + service); POS `useUpdateOrgTheme`/`ThemeContext`/theme-card rewired off the markets-api `org.theme`. (B) markets-api (`management-be`) standardized the org path segment `organization`→`organizations` across routes + swagger. **Incident+recovery:** the naive plural flip made org-CRUD `/organizations/{id}` and org-scoped `/organizations/{orgId}/...` sibling resources with different param names → API Gateway rolled back the gateway while the lambda had already updated (transient live 404s); reverted to restore service, then re-landed with the CRUD param unified `:id`→`:orgId` (one `/organizations/{orgId}` resource). Verified live: `/organizations/`→401, `/organization/`→403, theme PATCH→401. Deploy order sales-be→markets-api→POS, all green. Also fixed the sessions refetch storm (page_size 100→1, cached) and added native-arm matrix CI for data-be/store-be. |
| 2026-06-13 (CI perf + sessions storm) | **data-be CI parallelized** — the single-job emulated build of 34 images was unviable; replaced with a discover→matrix (one job/service) build on **native `ubuntu-24.04-arm`** runners (no QEMU) + `scripts/build-one.sh`; store-be switched to native arm too. data-be optimized run: 34/34 build jobs green + API Gateway deployed. Skill `gh-actions-aws-deploy` Variant C updated with the native-arm+matrix perf pattern. POS (`tsuru-pos-system`): fixed the **sessions refetch storm** — DashboardLayout `active-session` fetched `page_size=100` on every page with a 60s staleTime → now `page_size=1`, 5-min cache, no refetch-on-focus; SessionsPage list query given a staleTime. **Still open (markets-api / platform-api):** theme-select PATCH `/api/users/{u}/organizations/{id}` doesn't appear in the lambda logs → likely missing from the API-Gateway spec (gateway 404 before the lambda); org-settings "Pendiente" badges (branding/contact/payment/shipping) + theme read `org.settings`/`org.theme` which the org DTO doesn't populate/return. Both need a markets-api gateway-regen + DTO fix. |
| 2026-06-13 (data-be + store-be CI migration) | Migrated the last two backends off the old AWS CodePipeline to the Variant-C GitHub Actions OIDC flow (TSR-090 progress). **store-be** (cross-docking/orders `cd-backend`, repo `tsuru-store-be`, branch develop): single-job build→update→API-Gateway; deployed green and the previously-undeployed **branches** endpoint is now live (`GET /api/organizations/{id}/branches` → 401 not 404). *[Correction 2026-08-21 (TSR-139): this line originally read "branches/branch-types". Only `/branches` shipped — `branch-types` did not exist in any backend until TSR-139, and the gateway answered 403 "Missing Authentication Token" for it.]* **data-be** (multi-lambda, repo `tsuru-data-be`, branch main): 3-job build→update→API-Gateway. Both got a repo-scoped OIDC `deploy-role.yml` (bootstrapped via J-CAMPOS admin) + `AWS_DEPLOY_ROLE_ARN` secret; old CodePipeline stacks now superseded (retire separately). Repo identities use the NEW names (tsuru-data-be / tsuru-store-be), not the old CodePipeline SourceRepo values (biller-apps-data-services / cross-app-be). |
| 2026-06-13 (org-config 502 + FE polish) | **org-configurations 502 root-caused + fixed**: the service Docker image copied `jbiller_common` code but only installed its own `requirements.txt`, so the shared lib's transitive deps were missing → `Runtime.ImportModuleError` (requests→jinja2→pyyaml…) → 502 on `/configurations` + `/media`. Fix (`tsuru-sales-be` `4d5976d`): install the shared lib as a pip package (brings all its deps), still vendoring it at the task root for its data files; deploy green + verified via direct lambda invoke (import OK). POS (`tsuru-pos-system`): cached `useRegisteredOrganization` (5-min staleTime) to stop the cross-page refetch storm (`b9abaad`). Note: hacienda/notifications "Pendiente" badges keyed off the 502'ing endpoint and should clear now; branding/contact/payment/shipping badges + theme-on-relogin still need a platform-api org-DTO check (org entity DOES carry `theme`+`settings`). Skill `gh-actions-aws-deploy` gained **Variant C** (multi-lambda Python container deploy + shared-lib Docker pattern). Pending: migrate `tsuru-data-be` + `tsuru-store-be` (orders/branches) off CodePipeline to the Variant-C GH Actions flow (needs OIDC deploy-role bootstrap per repo). |
| 2026-06-13 (sales-be CI parity) | **Fixed `tsuru-sales-be` deploy** (`fc92eab`): the GH Actions workflow had drifted from the old CodePipeline into a manual/single-service/skippable-API deploy — the root cause of the org-config/media 404s and of "no auto-deploy on push." Rewrote `deploy.yml` to replicate CodePipeline 1:1: push→`develop` trigger + 3 always-run stages (build all → update all → regenerate API Gateway). Push triggered run `27470473107` (full deploy + gateway regen). §7 step 8 resolved. |
| 2026-06-13 (registration story) | **New: TSR-113 Done** — Sibö/cacao origin-story side panel added to POS registration (`fe/pos-system`: `AuthLayout` aside split, `BrandStoryPanel`, `auth.story.*` ES/EN, token-only styling; tsc+build green). Owner decision (2026-06-13) to ship a narrow, attributed cosmology slice; **TSR-045** updated — deeper Tier-2 (lexicon IA, Ú-sure/cacao logo mark, etnogeometric patterns) stays gated on a real RIBCA/Bribri partnership. |
| 2026-06-12 (rebrand R1–R3) | **TSR-041/042/043 Done** — JMarkets→Tsuru applied across the pages (no domain/ID changes). Landing (`fe/landing`): all content JSONs swapped, hero now leads with free e-invoicing, NEW `billing.json` pillar wired through the 4 admin pieces + `/facturacion` prerender route, honesty pass (fairs→"en construcción", testimonials/"miles"/phone/newsletter/24h removed), legal de-claim (GDPR/encryption), emails → `@tsuru-markets.org`, Tier-1 natural-pigment palette. POS (`fe/pos-system`): title/BRAND/Logo/login → Tsuru, theme display names + default palette recolored (IDs kept). Docs: root CLAUDE.md brand note + living-doc sweep. Executed via a file-partitioned orchestration workflow; both repos verified green (check/build/prerender, admin tree-shaken, 0 user-visible JMarkets). Deferred: logo/wordmark asset, domain/infra renames (TSR-044), Tier-2 Bribri (TSR-045). |
| 2026-06-12 (fe/ reorg) | **New: TSR-112 Done** — frontends grouped under `fe/` (mirror of `be/`): `landing-client`→`fe/landing`, `templates/pos-system`→`fe/pos-system` (gitignored standalone repos), `dashboard`→`fe/dashboard` (`git mv`, still tracked). `.gitignore` + monorepo path refs repointed (`package.json`, `tsconfig.json`, root `vite.config.ts`, `buildspec-frontend-dashboard.yml`, `deploys/deploy-params.sh`); dashboard build verified → `dist/dashboard`. Root CLAUDE.md split section updated + brand note added. |
| 2026-06-11 (deployment session) | **TSR-026 Done** (migration + seeds + org-modules backfill applied to live DB). **New: TSR-037** catalog realignment 1:1 with the POS sidebar (pos folded into `documents`; live reseed pending — §7 step 1), **TSR-038** POS nav RBAC gating + chepelcr@outlook.com→admin test mapping, **TSR-039** `ADMIN_AUTH_MODE=open` (temporary; admin Cognito pool planned), **TSR-104** server split → private `chepelcr/tsuru-platform-api` + green GH Actions OIDC Lambda deploy (monorepo CodePipeline backend stages now redundant), **TSR-105** pnpm migration, **TSR-106** CORS additions (tsuru.jcampos.dev, pos.j-markets), **TSR-051** brand asset guide. **TSR-046 Done** (landing CTAs + invitation emails → pos.j-markets). **TSR-035 superseded** by TSR-037. TSR-090/TSR-100 updated (3 folders split; all standalone repos pushed). §7 rewritten — remaining: catalog reseed, CodePipeline stage disable, TSR-028/029, enforce flip, admin pool. CLAUDE.md files (root + 3 split repos) now instruct keeping this doc updated. |
